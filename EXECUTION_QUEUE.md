# EXECUTION_QUEUE.md — Project-level sequential execution queue

**Owner directive 2026-05-29 (Batch 432):** *"We have been jumping all over the place and the items get missed. Lets bring order to the chaos."*

**Owner directive 2026-05-29 (Batch 444, lifecycle clarification):** *"Resolved items need to go down in the queue after being tagged as resolved. If needs a new iteration, needs be tagged as reopened and to be moved to top of queue. Add findings to the queue document as well. Note that such items need to be added in the queue document at each turn! This is mandatory!!!"*

**Purpose.** Single ordered list of the next project-level execution items. The **top of the queue is the next thing Claude executes**. Items can be reordered, but whatever sits at the top still executes next. This is the project-level analogue of per-session todos — it survives across sessions.

**Distinction from todos.** TodoWrite tracks in-session task state for *one* conversation. This queue tracks *project-level* obligations across sessions. A todo is "what am I doing in this conversation"; a queue item is "what is the project's next milestone."

---

## Rules

1. **Top item is next execution.** When work is needed and the queue is non-empty, the top `PENDING` (or `REOPENED`) item runs next.
2. **Mandatory update each turn (CHECKLIST #94).** Every turn that produces meaningful changes — INCLUDING newly-discovered findings, bugs, follow-ups — must end with a queue sweep. Failure to update = non-compliance. Discoveries become queue items the SAME turn they're discovered, not "next time".
3. **Reorder freely.** Owner or Claude can reorder via Edit. The new top runs next.
4. **DEFERRED keeps its position.** A `DEFERRED` item is skipped *without losing its slot*. The next non-deferred `PENDING` item runs first; the deferred item stays where it was so it surfaces again next time the queue is consulted.
5. **RESOLVED moves to the bottom of the active queue (not deleted).** When an item is finished, tag it `RESOLVED` and move it to the end of the active queue table (with the resolution batch / commit / one-line outcome in Notes). Keeping it visible in the active queue — not just a log line — makes the resolution traceable when the same area is touched later. Only TRULY archived items (never coming back) move to the Completed log section.
6. **REOPENED moves to the top.** If a RESOLVED item needs another iteration (regression, incomplete fix, new evidence), tag it `REOPENED` and move it to row #1 of the active queue. Update Notes with what changed.
7. **Status enum.**
   - `PENDING` — queued, not yet started.
   - `IN_PROGRESS` — actively being worked on this session.
   - `BLOCKED` — cannot proceed without an external unblock (waiting on owner / external system / dependency); explain blocker in Notes.
   - `DEFERRED` — owner-directed skip-without-loss; retains queue position.
   - `RESOLVED` — finished + moved to bottom of active queue. Stays visible.
   - `REOPENED` — was RESOLVED, surfaced as needing another iteration; moves to top.
   - `DONE-ARCHIVED` — truly done, never coming back; moved out of active queue into Completed log.
8. **One IN_PROGRESS at a time.** Like TodoWrite, only one item is `IN_PROGRESS` at any moment so the focus is unambiguous.
9. **Item identifiers stable.** Use a short kebab-case slug per item (`smc-silent-failure-rootcause`) so the same item is referenceable across turns even after reorder.
10. **No silent drops.** If an item is removed without RESOLVED tag, explain the reason in the Completed log (e.g., `dropped — superseded by Y`).
11. **Owner authority.** Owner can add, reorder, defer, resolve, reopen, or drop any item by message. Claude cannot drop an item without owner instruction.
12. **Findings become items immediately.** Anything discovered in a status report ("X was supposed to drop but didn't / Y stayed at 100 / Z needs investigation") becomes a queue row that same turn. Don't let findings dangle in chat history.

---

## Active queue (top = next; RESOLVED items sink to the bottom of this table, not removed)

| # | Slug | Item | Status | Notes |
|---|---|---|---|---|
| S5-SQUEEZE-EVENT-ONLY-AB | `squeeze-setup-event-only-l1c-offline-ab` | **A/B test of EVENT-only L1c vs OR composite in `strat_squeeze_setup_long`** (B620 deletion artifact per `R5_VALIDATION_MANIFEST.md` M3). B615 added strat_squeeze_setup_event_only_long as B-twin; B619 fire-count estimator flagged FAIL_FIRE_STARVED (~2.5 fires/yr UB); B620 DELETED the B-twin. The A/B question is answerable OFFLINE post-R5 from strat_squeeze_setup_long's trade log: filter trades to subset where `insider_cluster_active=True` at fire bar + compare hit-rate / Sharpe of EVENT-subset vs full population. If EVENT-subset materially outperforms, consider re-introducing as smaller A/B or loosening L1c to EVENT-only with less aggressive L3. | DEFERRED-STAGE-5 | **B620 (2026-06-08):** B-twin deleted; A/B scope preserved as offline trade-log analysis. **B632 (2026-06-08 owner directive):** added to queue per "Other M2-M8 items from R5_VALIDATION_MANIFEST.md". Requires post-R5 trade_log.csv with sufficient fire count for OR composite. |
| S5-FIRE-COUNT-CANDIDATES | `fire-count-fail-warn-empirical-validation` | **9 strategies flagged by B621 fire-count estimator audit** (5 REAL FAIL + 4 WARN; per `R5_VALIDATION_MANIFEST.md` M4). R5 cube must produce actual fires/year per strategy to validate or refute estimator's independence-product upper bound. For each REAL FAIL: if cube produces >=30 trades/regime, estimator was too pessimistic (correlated gates fire more often than independent product); re-tune PRIOR_RATES. If <30 trades/regime, owner decides per `feedback_minimum_fire_count_gate_before_cube` resolutions (loosen / mark exploratory / delete per B620 precedent). **REAL FAIL (5)**: volume_spike_breakout_retest (0.01/yr, 9 gates), volume_spike_breakout (0.07/yr, 9 gates), break_retest_confluence (0.09/yr, 11 gates), 52wl_break_retest_short (0.13/yr, 9 gates), break_retest_volume (1.54/yr, 7 gates). **WARN (4)**: news_momentum_short (8.73/yr), flag_bear_retest_short (15.77/yr), news_momentum_long (20.79/yr), donchian_breakdown_short (24.95/yr). | DEFERRED-STAGE-5 | **B621 (2026-06-08):** estimator audit produced verdicts; no auto-action per `feedback_no_apriori_strategy_pruning` + `feedback_no_rushing_per_strategy_tweak`. **B632 (2026-06-08 owner directive):** added to queue per R5 manifest M4. R5 cube empirical confirms or refutes. |
| S5-OR-AWARE-ESTIMATOR | `or-aware-fire-count-estimator-extension` | **OR-aware extension to `scripts/estimate_fire_count.py`** per `R5_VALIDATION_MANIFEST.md` M5. Current AND-product estimator over-restricts strategies with OR composites (squeeze_setup_long L1c + L2 OR; smart-money sleeves via `_has_smart_money_buy` helper). B621 audit harness demoted these to FAIL_FIRE_STARVED_BUT_HAS_OR for manual review (1 strategy currently: squeeze_setup_long false-FAIL at 0.12/yr UB). Either (i) post-R5 actual fire-count makes the AND-OR distinction moot (cube empirical IS the verdict) OR (ii) extend estimator to compute joint probability with explicit OR clauses for more useful pre-cube projections. | DEFERRED-STAGE-5 | **B621 (2026-06-08):** OR-flag demotion implemented as workaround. **B632 (2026-06-08 owner directive):** added to queue per R5 manifest M5. Owner decides priority post-R5; cube data may render this redundant. |
| S5-PRIOR-RATES-EXPANSION | `prior-rates-expansion-from-r5-signal-frequencies` | **PRIOR_RATES expansion for 189 INCOMPLETE_PRIORS strategies** per `R5_VALIDATION_MANIFEST.md` M6. B621 fire-count audit classified 189 of 221 strategies as INCOMPLETE_PRIORS because their gate signals aren't in `scripts/estimate_fire_count.py:PRIOR_RATES`. Examples: institutional_persistence_*, classification_change_*, mmbm_long, judas_swing_*, week_opening_gap_*, vix_*, sector_*, calendar_* (totm_long, halloween_seasonal_long, etc.). Post-R5, measure actual per-signal fire rates from `signal_fire_rates.json` (engine output) + back-fill PRIOR_RATES with measured priors. After back-fill, re-run `scripts/audit_fire_counts.py` to surface additional FAIL/WARN candidates that were previously hidden. | DEFERRED-STAGE-5 | **B619 (2026-06-08):** estimator built with hand-curated 60-signal PRIOR_RATES table; INCOMPLETE_PRIORS flags gaps. **B632 (2026-06-08 owner directive):** added to queue per R5 manifest M6. Requires R5 engine output with signal_fire_rates.json for back-fill. |
| S5-RSI-DEFAULT-50-FAMILY | `rsi-default-50-silent-gap-family-grep` | **`s.get("rsi_14", 50)` default-value silent-gap family** per B637 walk critique #6 (owner-approved F5 2026-06-09). The default=50 pattern is fail-safe ONLY for gate constructions where 50 lies OUTSIDE the threshold band (e.g., `< 45` or `> 55`). For any threshold spanning 50 (e.g., `< 55` or `> 45`), default=50 auto-passes the gate on missing data — silent-gap class hiding in numeric defaults rather than `not s.get(...)` boolean NOTs (which B608-B633 already swept). Same hazard for `adx`, `atr`, anything with `.get(key, neutral_default)`. **B639 scope:** grep `\.get\(.+,\s*\d+\.?\d*\)` across `screener.py`; identify strategies where the default value falls inside the gate's accept band; either require the key (`s["rsi_14"]` with caller guarantee) or default to a value that fails any reasonable threshold (NaN with explicit None-guard, or 0/100 for RSI to force a known-fail state per gate direction). | DEFERRED-STAGE-5 | **B639 (2026-06-09 owner-approved F5):** family identified during strat_morning_star walk critique #6. Not auto-fixed in B639 because (a) per-strategy verification needed (no mass-edit safe); (b) some current gates rely on fail-safe behavior and changing defaults could mask legitimate signals. Owner decides per-strategy resolution post-R5 when failure-mode trade logs are available. |
| S5-FIRE-COUNT-MEASURED-RUN | `fire-count-measured-vs-independent-product-replacement` | **Fire-count measurement pass — replace `scripts/estimate_fire_count.py` independent-product upper-bound with measured fires/year/direction against actual 220-ticker history** per B640 external-AI audit methodology-findings #1+#2 (owner 2026-06-09 directive #1). Current estimator assumes gate independence; reality: at structural co-occurring extremes (S3 capitulation = near_s3+rsi<30+vol_spike all co-fire by construction; R3 breakout = above_r1+macd_bullish+above_avwap all co-occur in established trends) the joint probability >> product of marginals → FAIL_FIRE_STARVED labels under-bias. Conversely at structurally exclusive gates the product over-bias. Both directions are systematic, not random. **Tool spec:** `scripts/measure_fire_count.py` accepts gate-set spec (list of signal-key names + threshold ops) + universe slice + date range; emits actual fires/(ticker × bar × direction) tally + pairwise gate-correlation matrix. **First run target:** all 10 B640 walk strategies + the 5 FAIL_FIRE_STARVED labels from B621 audit (volume_spike_breakout_retest, volume_spike_breakout, break_retest_confluence, 52wl_break_retest_short, break_retest_volume) — converts every B640 fire-count verdict from guess to fact. Documentation appended to `STAGE_4_BATCH_640_WALK_BUNDLE.md`. **Why now not Stage 5:** the independent-product is currently driving loosen/defer recommendations on 5 of 10 B640 strategies (W2/W5/W6/W7/W8). Measured numbers adjudicate the next walk bundle's recommendations + every prior FAIL_FIRE_STARVED claim. | IN_PROGRESS_B641 | **B640 walk bundle (2026-06-09):** external-AI audit caught the independence-assumption bias direction; owner directive #1 promotes from S5 to immediate build. Following up B641 narrow-scope Tier 1 ship. |
| S5-MULTIPLE-TESTING-CORRECTION | `program-level-deflated-sharpe-fdr-spa` | **Program-level multiple-testing correction on the cube selection step** per B640 external-AI audit C2 (owner 2026-06-09 capture audit). 220 strategies × ~26 exit methods × multiple regimes = thousands of cells; "let the cube decide" with no correction overfits by construction. Three established methods: (a) **deflated Sharpe ratio** (Bailey & López de Prado 2014 *Journal of Portfolio Management* — adjusts the candidate Sharpe by the number of trials and the variance of Sharpes); (b) **Hansen Superior Predictive Ability (SPA) test** (Hansen 2005 *JBES* — bootstrap-based, tests whether the best strategy genuinely outperforms a benchmark accounting for multiple comparisons); (c) **Benjamini-Hochberg FDR** (1995 *JRSS-B* — controls false-discovery rate at α=0.05 with rank-ordered p-values). Implementation: post-cube + post-walk-forward, before any deployment gate. **Why critical:** the fire-count gate addresses too-few-trades; this addresses too-many-strategies. Without it, cube backtest Sharpes are inflated by ~√(2·ln(N)/n) (Bailey-LdP haircut). | DEFERRED-STAGE-5 | **B640 (2026-06-09):** flagged in external-AI audit as the biggest scientific gap. Pairs naturally with C3 marginal-contribution scoring (next ticket). |
| S5-MARGINAL-CONTRIBUTION-SCORING | `cube-scores-strategies-by-marginal-vs-book-not-standalone` | **Cube ranks strategies by marginal-vs-book contribution, not standalone Sharpe** per B640 external-AI audit C3 (owner 2026-06-09 capture audit). 220 strategies built from a shared ~15-feature OHLCV-derived basis; correlation is structural. Aggregate backtest equity shows smoother-than-live behavior because correlated signals produce correlated drawdowns clustered in the same regimes. **Extends existing M9 effective_strategy_count via clusters:** M9 already collapses correlated strategies into effective-count for tier sizing; this ticket extends the methodology to cube-scoring itself — when evaluating "should we add strategy X to the book?" the metric is `book_with_X.sharpe - book_without_X.sharpe`, NOT `X.standalone_sharpe`. **Implementation:** scoring pass after multiple-testing correction; iterative greedy add/drop with marginal-IR computed against the rolling book. Lavin-Tetlock 2019 portfolio-construction style approach. | DEFERRED-STAGE-5 | **B640 (2026-06-09):** identified as C3 in external-AI audit. Cross-ref M9 (RESOLVED clusters via correlation matrix); this ticket promotes from sizing-only to selection methodology. |
| S4-CORPORATE-ACTION-POLICY | `corporate-action-handling-explicit-policy-plus-ex-date-test` | **Explicit corporate-action (splits, dividends, spinoffs) handling policy + ex-date no-fire pyramid test** per B640 external-AI audit C4 (owner 2026-06-09 capture audit). Pattern: unadjusted splits create a one-day "gap" trivially satisfying engulfing patterns (`today_open < prev_close` is auto-True on a 2-for-1 split) + blow out pivots + near_X proximity. Ex-dividend gaps do the same at smaller scale. **Required deliverable:** (a) verify Polygon OHLCV adjustment policy (adjusted vs unadjusted feed + retroactive vs forward); (b) ex-date detection helper (`is_ex_date(ticker, date)` using existing `data_prefetch/polygon/dividends/` cache); (c) pyramid test: for every candle pattern (engulfing, doji, hammer, shooting_star, morning_star, evening_star, three_white_soldiers, three_black_crows) + every pivot proximity (near_*) signal, assert no fire on a known historical ex-date (e.g. AAPL 2014-06-09 7-for-1 split, MSFT 2003-02-18 2-for-1, recent NVDA 2024-06-10 10-for-1); (d) explicit code-level corporate-action policy documented in `backtest/data/fetcher.py`. **Severity:** systematic-not-rare (every dividend-payer has 4 ex-dates/year; every split is one phantom-signal event); contaminates exactly the strategies the candle + pivot walks depend on. | DEFERRED-NEAR-TERM | **B640 (2026-06-09):** identified as C4 in external-AI audit. Pre-cube data integrity issue; should resolve before R5 cube run trusts candle / pivot signals. |
| S4-SURVIVORSHIP-T1A-VERIFY | `survivorship-bias-verify-t1a-includes-delisted-during-window` | **Verify T1a PIT universe includes delisted-during-window names + cross-ref DEC-477** per B640 external-AI audit C5 (owner 2026-06-09 capture audit). Survivor universes systematically delete the left tail — names that went to zero (bankruptcy, fraud, terminal decline) are absent from "currently-interesting" lists. Capitulation-buying expectancy (W5 `strat_pivot_s3_capitulation`, all deep-dip longs) is left-tail-dominated; survivorship is structurally fatal. **Verification scope:** (a) confirm `Backtesting universe/Tier 1A Universe_SP500 Tickers_Jan 2020 to May 2026.csv` includes the 111 "removed-during-window" rows per DEC-477 (already documented; verify count + spot-check 5 known delistings: BBBY, SVB, FRC, SBNY, FTX — though several may be T2 rather than T1a); (b) confirm engine PIT loader filters `(added_date IS NULL OR added_date <= as_of) AND (removed_date IS NULL OR removed_date > as_of)` so backtests with as_of=2022-12-01 actually see BBBY which had not yet delisted; (c) re-run W5 capitulation backtest on PIT universe vs current snapshot universe — measure left-tail damage; (d) if T1a is clean, T2/T3 may carry residual survivorship since they're momentum-screener outputs. | DEFERRED-NEAR-TERM | **B640 (2026-06-09):** identified as C5 in external-AI audit. DEC-477 indicates T1a is PIT-canonical but verification has not been adversarially tested for the strategies most dependent on left-tail integrity. |
| S4-W4-F1-PLUS-F2-PLUS-RSI-MISLABEL | `pivot-s2-bounce-symmetry-doc-rsi-relabel` | **strat_pivot_s2_bounce follow-on B641 split per CHECKLIST (g) sequence-or-split.** Three changes queued separately to avoid simultaneous bundling: (a) F1 — add `shooting_star` to SHORT OR-disjunct symmetric to LONG's `hammer OR bullish_engulfing` (SHORT pre-B641 only had `bearish_engulfing`); one-line additive to existing OR. (b) F2 — docstring add. (c) RSI mislabel correction — context bullet calls RSI<40 "oversold" but canonical Wilder oversold is 30; relabel to "below-neutral" or "soft-oversold" to avoid Step-4 thesis inflation. Per `feedback_no_rushing_per_strategy_tweak` each ships in its own owner-direction turn. | PENDING_OWNER_BATCH | **B641 (2026-06-09):** F3 regime-delete shipped Tier 1; F1/F2/RSI-mislabel split off to follow per CHECKLIST (g). |
| S4-W6-W7-W8-LONG-DEFAULT-TRUE-UNIFY | `avwap-long-default-true-asymmetric-severity-unify` | **W6/W7/W8 LONG-side AVWAP default-True silent-gap family — severity unification with W8 F1b** per B640 external-AI audit methodology-finding #5 (owner 2026-06-09 capture audit). All three strategies had LONG-side `s.get("above_avwap_*low", True)` (default True) which AUTO-PASSED the AVWAP gate when key missing. Same auto-pass-on-missing class as W8's F1b (B641) + T8 weekly Kumo (B657). | **RESOLVED-B659** | **B641:** queued. **B657:** T8 weekly Kumo portion resolved (option D from T8 audit). **B659 (2026-06-09 owner-directed autonomous implementation):** W6 + W7 + W8 LONG AVWAP defaults swapped True -> False symmetric with SHORT side. All three pivot strategies now fail-safe to no-fire when AVWAP signals are absent. Same fix template as B641 W8 F1b + B657 T8 weekly Kumo. |
| S4-T3-NOT-ABOVE-200-EMA-PATTERN | `hull-rsi-short-not-above-200-not-pattern-fix` | **strat_hull_rsi SHORT side uses `(not above_200)` NOT-pattern silent-gap** where `above_200 = s.get("price_above_ema_200", False)`. | **RESOLVED-B659** | **B656:** surfaced during T3 redundancy audit; not bundled with A+C per CHECKLIST (g). **B659 (2026-06-09 owner-directed autonomous implementation):** swapped `(not above_200)` -> `s.get("below_ema_200", False)` positive symmetric (B630 producer-additive). Same template as B641 W8 F1b. |
| S4-W5M-SYMMETRIC-VOL-GATE | `w5m-add-vol-below-avg-symmetric-to-w5-b650` | **W5m (`strat_pivot_r3_blowoff_short`) needs the symmetric `vol_below_avg` AND-gate** per B650 W5 LONG addition (Wyckoff Distribution Upthrust-Test requires low-volume failed-upthrust bar, mirror of W5 Spring low-volume Test). | **RESOLVED-B659** | **B653:** identified. **B659 (2026-06-09 owner-directed autonomous implementation):** added `s.get("vol_below_avg")` AND-required on W5m reversal-trigger bar. Strategy now properly distinguishes a Wyckoff Upthrust-Test (low-volume failed retest of BC high) from a continuation rally on heavy accumulation volume. Mirror of B650 W5 Spring volume condition. |
| S4-W8-RSI-NOOP-GATE | `cpr-narrow-bullish-rsi-50-near-no-op-gate` | **strat_cpr_narrow_bullish `rsi>50/<50` strict-inequality on default-50 is a near-no-op gate** per B640 external-AI audit per-strategy finding (owner 2026-06-09 capture audit). The gate removes roughly half the sample but adds almost no information (RSI strictly above/below midpoint is a coin-flip-level filter, not a meaningful momentum gauge). Also accidentally-fail-safe on default=50 because strict inequality on midpoint (default value AT threshold) — so the strategy escapes the S5-RSI-DEFAULT-50-FAMILY hazard by luck, not design. Resolution options: (a) drop the gate entirely (cleaner, slightly raises fire count); (b) tighten to canonical bands (`>55` LONG / `<45` SHORT) to add real information; (c) keep + document as accidentally-safe. | **RESOLVED-B654** | **B641 (2026-06-09):** queued from external-AI audit. **B654 (2026-06-09 owner-directed W8 redundancy-audit option B-local):** RESOLVED via option (a) -- `rsi_14>50/<50` gates dropped from `strat_cpr_narrow_bullish` (both directions). Resolved jointly with S4-W8-REDUNDANCY-AUDIT. |
| S4-OBV-LOCATION-TENSION-DESIGN | `obv-vs-location-tension-w1-w3-w9-design-question` | **OBV-vs-location anti-correlation design question** per B640 external-AI audit per-strategy finding (owner 2026-06-09 capture audit). Affects W1 (`bullish_engulfing_support`), W3 (`pivot_s1_bounce`), W9 (`camarilla_s3_bounce`) LONG sides + mirrors on SHORT. Pattern: the strategy fires LONG at support after a fresh decline. By construction, a "fresh decline into support" means OBV has been falling, so `obv_bullish = (obv > 20-bar mean)` is likely FALSE exactly when the setup is most genuine. The OBV gate FIGHTS the support premise — it either suppresses the truest signals (rejecting the freshest declines) or selects for a contradictory subpopulation (at-support-but-OBV-already-turned, which means decline was already absorbed). Three design resolutions: (a) drop OBV gate from these strategies — accept lower confluence in exchange for not fighting the thesis; (b) reframe OBV to `obv_diverge_bull` (which already exists at technical.py:1157: `c.iloc[-1] < c.iloc[-5] AND obv.iloc[-1] > obv.iloc[-5]` — bullish divergence between price and OBV; matches the "fresh decline + accumulation footprint" thesis correctly); (c) keep as designed + accept the tension as a deliberate filter for "high-quality" bounces where OBV is already showing absorption. Owner decides. | PENDING_OWNER_DESIGN_CALL | **B640 (2026-06-09):** identified as cross-strategy design question in external-AI audit. Solution (b) is cleanest — `obv_diverge_bull` exists as producer + matches the thesis. |
| S4-FIB-ANCHOR-LOOKAHEAD-AUDIT | `at-key-fib-swing-anchor-unspecified-audit` | **`at_key_fib` swing-anchor selection auditing** per B640 external-AI audit per-strategy finding W1 (owner 2026-06-09 capture audit). `compute_fibonacci` at technical.py:164 uses `lookback=50` default + `sh = w["high"].max(); sl = w["low"].min()` over the trailing window. The swing anchor (which `sh` / `sl` define) is selected from a 50-bar trailing window. **Risk:** if the engine ever calls `compute_fibonacci` with a window that includes bars after `as_of`, the swing anchor would peek into the future. Need explicit verification that callsite (screen_instrument / signal computation path) slices df to `df[df.index.date <= as_of]` before passing to `compute_fibonacci`. Also: `lookback=50` is a hidden free parameter — if changed, every consumer of `at_key_fib` changes behavior silently. Document the parameter + add a pyramid test that `compute_fibonacci(df_with_future_bars, lookback=50)` returns IDENTICAL `at_key_fib` to `compute_fibonacci(df_sliced_to_as_of, lookback=50)`. | PENDING_PIT_AUDIT | **B640 (2026-06-09):** identified as cross-strategy lookahead-vector in external-AI audit. Low confidence in actual lookahead (engine slicing convention is well-established) but no explicit test exists. |
| S5-REGIME-BETA-ASSUMPTION | `market-regime-sizes-single-stock-strategies-beta-assumption` | **Documentation + design proposal: market-regime sizing of single-stock strategies carries unstated beta assumption** per B640 regime-classifier audit finding #1 (owner 2026-06-09 capture audit). `classify_regime` reads ONLY SPY + VIX (broad market). The output then gates + sizes 220 single-stock strategies as if every stock shares the market's regime. False when it matters most: in `bull` market a specific stock can be in its own private bear (post-earnings collapse, sector rotation, idiosyncratic blowup); engine sizes that name's long at full + permits short→long conversion. Conversely a strong stock in a `bear` tape gets sized to 0.5× for reasons unrelated to it. **Severity:** concentrated, not diffuse — injects a systematic market-beta tilt into nominally stock-specific signals, strongest when single-name dispersion is highest (earnings season, sector rotation). **Resolution options (R5+):** (a) name the assumption explicitly in CLAUDE.md + dashboards (minimum); (b) per-sector regime (read sector-ETF instead of SPY for stocks in that sector — XLK regime for tech names etc); (c) per-name regime (use the stock's own 200-EMA + IV instead of SPY's). Each adds complexity; cube validates which is worth the cost. | DEFERRED-STAGE-5 | **B640 (2026-06-09 regime classifier audit):** unstated modeling assumption. R5 walk-forward classifier validation (separate ticket S5-REGIME-WALK-FORWARD-VALIDATION) should test sector-vs-market-regime as part of that work. |
| S4-REGIME-AAII-PIT | `aaii-publication-vs-survey-date-pit-audit` | **AAII sentiment publication-date vs survey-date PIT alignment audit** per B640 regime-classifier audit finding #4a (owner 2026-06-09 capture audit). AAII weekly sentiment is surveyed Wed→Wed; published Thursday. If `compute_bear_composite_score` reads `data_prefetch/aaii/weekly_sentiment.parquet` and filters by survey-period date rather than publication date, the composite peeks ~2-7 days into the future on every bear-composite evaluation. Lookahead is subtle (small magnitude, varies by week) but systematic across all backtest dates. **Required test:** pyramid pin asserting AAII `compute_bear_composite_score(as_of=2024-03-13)` returns the value PUBLISHED on or before 2024-03-13 (likely the 2024-03-07 survey), not the 2024-03-13 survey (which publishes 2024-03-14). | DEFERRED-NEAR-TERM | **B640 (2026-06-09):** identified as alt-data alignment classic lookahead. |
| S4-REGIME-FRED-VINTAGE | `fred-t10y2y-vintage-revision-pit-audit` | **FRED T10Y2Y vintage-revision PIT integrity audit** per B640 regime-classifier audit finding #4b (owner 2026-06-09 capture audit). FRED serves the latest-vintage (current) yield-curve data by default; series may be revised after publication date. Backtest using current FRED history sees values as known TODAY, not as known on the bar date. T10Y2Y revisions are typically tiny (cents-on-the-dollar Treasury yields), but the principle (using restated series PIT-unsafely) is the same bug class that would be fatal elsewhere. **Required deliverable:** confirm FRED data fetch policy (vintage vs current) in `backtest/data/macro.py`; if current, document the lookahead-magnitude as a known limitation; consider switching to ALFRED (archival FRED) which serves vintage-as-of-date data. | DEFERRED-NEAR-TERM | **B640 (2026-06-09):** identified in external-AI audit. Cross-ref Sprint 0A FRED + ALFRED data fetch (per CLAUDE.md). |
| S4-REGIME-SECTOR-ELIGIBILITY-TIME-VARYING | `sector-breadth-bear-composite-time-varying-eligibility` | **Sector-breadth bear-composite eligibility is time-varying** per B640 regime-classifier audit finding #4c (owner 2026-06-09 capture audit). `compute_bear_composite_score` requires sector ETFs to have ≥200 bars to be eligible; early in backtest history fewer than 5 of 8 ETFs qualify, so the breadth indicator silently contributes 0 — meaning the bear-composite override is WEAKER in early history (max score 1 from yield-curve alone, never reaching score≥2 threshold) and STRONGER later. Classifier sensitivity is time-varying; early-period and late-period backtest regimes are non-comparable. **Required test:** pyramid pin verifying which dates `compute_bear_composite_score(as_of=...)` has full 3-indicator support vs partial; document the time-varying-classifier window in CLAUDE.md. | DEFERRED-NEAR-TERM | **B640 (2026-06-09):** identified. Linked to S5-REGIME-WALK-FORWARD-VALIDATION (the validation must hold the eligibility-window constant or report broken-out results pre/post-eligibility-threshold-date). |
| S4-REGIME-COMPOSITE-FAIL-POLICY | `bear-composite-missing-equals-zero-fails-open-vs-system-fails-closed` | **Bear composite "missing input contributes 0" is fail-OPEN; contradicts system's fail-closed VIX policy** per B640 regime-classifier audit finding #5 (owner 2026-06-09 capture audit). DEC-316 (Pass 51) made VIX-missing → unknown → block all entries: fail-CLOSED. But `compute_bear_composite_score`'s docstring says "Each indicator falls back gracefully to False when data unavailable… score==0 in that case means no override - regime falls back to classify_regime VIX/SPY ladder." That's fail-OPEN: when AAII + breadth are unavailable (early backtest), composite can score at most 1 (yield curve alone), so Batch 292 stealth-bear protection is SILENTLY OFF for the early period. Owner decision: (a) keep current fail-open (composite missing → fall back to VIX/SPY ladder which has its own fail-closed via DEC-316); (b) switch to fail-closed when ≥2 of 3 indicators missing (forces "unknown" or "bear" by default); (c) document the asymmetry explicitly + accept it. The "can't false-trigger" framing optimizes against false bears at the cost of false non-bears — for a risk-reduction override, false non-bears are the more dangerous direction. | PENDING_OWNER_POLICY_DECISION | **B640 (2026-06-09):** identified. Owner-policy decision required; not a code bug per se. |
| S4-REGIME-HYSTERESIS-PARITY-TEST | `hysteresis-opt-in-backtest-vs-analytics-parity-test` | **Hysteresis is opt-in via `use_hysteresis` flag — backtest-live parity risk** per B640 regime-classifier audit finding #7 (owner 2026-06-09 capture audit). Engine wires hysteresis ON in production paths; one-shot helper calls (analytics, dashboards) can default OFF. The regime label SHOWN in analytics for a given day may differ from the regime ACTUALLY USED during backtest for that same day. Every regime-affinity finding in the B640 walks implicitly assumed it was reasoning about the same regime series the engine traded under. **Required pyramid pin:** for each major callsite of `classify_regime` / `get_regime_context`, assert `use_hysteresis=True` is passed (production callsites) OR explicitly justify `use_hysteresis=False` (one-shot/help-text callsites). Audit `grep -r "classify_regime\|get_regime_context" backtest/` and verify per callsite. | DEFERRED-NEAR-TERM | **B640 (2026-06-09):** identified as silent-divergence risk between backtest engine + analytics layer. |
| S5-REGIME-WALK-FORWARD-VALIDATION | `regime-classifier-walk-forward-validation-curve-fit-check` | **Walk-forward validation of the regime classifier itself: is regime-gating net-positive out-of-sample?** per B640 regime-classifier audit finding #8 (owner 2026-06-09 capture audit). Classifier complexity has accreted across Batches 288/292/317/388 with thresholds (40, 30, 20, score≥2, 5-point buffer, 5-day SMA, ≥5 sectors, AAII 40%) each tuned to a specific historical failure (2022, Aug-2022 rally, single-day flips). That's textbook curve-fit. **Test:** freeze the classifier as of each historical date (i.e. the classifier as it would have been defined by then) + measure forward regime-gating value. If post-2022-fix performance on data 2022 hadn't seen (e.g. 2024-2025) is materially worse than the in-sample 2022 fix value, classifier is overfit. Cited –275pp 2022 figure is in-sample post-mortem; honest evaluation needs OOS. **B649 update:** also gates the B642 R3 EMA-cross 2% hysteresis -- per 2nd-critique #4 that's an unvalidated directional bet (sticky-bear bias) not a robustness fix; walk-forward determines whether sticky-bear beats symmetric OOS. | DEFERRED-STAGE-5 | **B640 (2026-06-09):** identified as meta-question — does the regime gating apparatus earn its keep? R5 scope; separate from R5 strategy-cube validation. **B649 (2026-06-09):** explicitly scoped to also validate R3 hysteresis asymmetry. |
| S5-FIRE-COUNT-MEASURED-RUN-FULL | `fire-count-measured-run-full-universe-multi-regime` | **Authoritative fire-count measurement: full T1a (--max-tickers 0) across multiple regimes** per 2nd-wave external-AI critique #1 (owner 2026-06-09). The B641/B648 smoke runs (20-ticker × 2022-2024 then random-30 × 2022-2024) are PRELIMINARY per the doc's own non-representativeness caveats; specific verdict-reversals are HYPOTHESES not results. Need a full-universe run for authoritative numbers + multiple regime arcs (2020 V-shape + 2018-19 + 2022 + 2024) to validate per-regime clustered counts rather than calendar-year-smeared annualizations. Likely overnight runtime; the B648 `--max-tickers 0` + `--ticker-sample-strategy all` path supports this. | DEFERRED-NEAR-TERM | **B648 (2026-06-09):** tool ready (`scripts/measure_fire_count.py` post-B648 hardcoded-220 scale-factor fix). Smoke results in B641/B643/B645/B648 runs are PRELIMINARY; this ticket is the authoritative gate before any verdict-reversal claim is treated as a result rather than a hypothesis. |
| S4-W5M-SYMMETRIC-VOL-GATE | `w5m-add-vol-below-avg-symmetric-to-w5-b650` | **W5m (`strat_pivot_r3_blowoff_short`) needs the symmetric `vol_below_avg` AND-gate** per B650 W5 LONG addition. The B650 fix added `vol_below_avg` to W5 LONG's reversal-trigger bar (Bulkowski/Wyckoff Spring requires low-volume Test). The Wyckoff Distribution mirror (Buying Climax → Automatic Reaction → Upthrust-Test) ALSO requires low-volume on the failed-upthrust bar (supply-was-absorbed mirror of demand-was-absorbed). B645 wired W5m without this gate; B650 added it to W5 LONG but not the SHORT mirror — symmetry gap. **Action:** edit `strat_pivot_r3_blowoff_short` to add `s.get("vol_below_avg")` AND-required on the bar of fire. One-line additive change + test pin updates. | PENDING_OWNER_DIRECTION | **B653 (2026-06-09):** identified during pivot doc update post-measurement landing. Owner directs whether to ship now or defer with the rest of W5/W5m work. |
| S4-TREND-REDUNDANCY-AUDIT | `trend-cluster-redundancy-audit-hull-ichimoku-supertrend` | **3 trend-cluster strategies fire every 2.5-4 trading days per ticker on the random-30 sample with all-STATE gates** (B648 random-30 sample). Per the B649-corrected critique #2 methodology, ASK PER-GATE "what does this screen out that the others don't?" Outcomes differ: extreme NO-OP (W8/T10) vs honest CONFLUENCE (T3, T8) vs default-True asymmetric (W6/W7/W8 + T8 weekly Kumo). | RESOLVED-B655-B656-B657 | **B653:** identified. **B655 (T10 option B):** EVENT-anchored lookback replaced supertrend_bullish NO-OP. **B656 (T3 option A+C):** honest confluence confirmed; only RSI noop dropped. **B657 (T8 option E = A+D):** honest confluence confirmed AND default-True weekly Kumo silent-gap fixed (default-True -> default-False on both weekly_above_cloud / weekly_below_cloud); resolves T8 portion of `S4-W6-W7-W8-LONG-DEFAULT-TRUE-UNIFY` as a free-rider (W6/W7/W8 portions still pending). All 3 originally-flagged trend strategies now redundancy-audited and addressed per their actual pattern. |
| S4-T3-NOT-ABOVE-200-EMA-PATTERN | `hull-rsi-short-not-above-200-not-pattern-fix` | **strat_hull_rsi SHORT side uses `(not above_200)` NOT-pattern silent-gap** where `above_200 = s.get("price_above_ema_200", False)`. Same class as W8 F1b fixed in B641. When `price_above_ema_200` key is missing, default False -> `not False = True` AUTO-PASSES the SHORT gate. Fix per `feedback_never_use_NOT_s_get_pattern`: swap to `s.get("below_ema_200", False)` (B630 producer-additive symmetric pair). **B656 (2026-06-09):** surfaced during T3 redundancy audit but NOT bundled with A+C per CHECKLIST (g) sequence-or-split. Owner-direction needed. | PENDING_OWNER_DIRECTION | **B656 (2026-06-09):** identified during T3 hull_rsi redundancy audit. One-line additive fix; same B641 W8 F1b precedent. |
| S4-W8-REDUNDANCY-AUDIT | `w8-cpr-narrow-bullish-gate-redundancy-audit` | **W8 cpr_narrow_bullish fires every 4 trading days per ticker (~34,000/yr universe-wide post-B648 corrected scaling) with 5 STATE gates that all measure 'established uptrend in different forms'**. Per B649-corrected critique #2 methodology, this is REDUNDANCY (gates over-determined) not confluence. | **RESOLVED-B654 (option B-local)** | **B649 (2026-06-09):** identified during methodology-takeaway inversion. **B653 (2026-06-09):** cross-ref to S4-TREND-REDUNDANCY-AUDIT. **B654 (2026-06-09 owner-directed option B-local):** RESOLVED via: (1) NEW producer `cpr_narrow_tight` in compute_pivots (0.05 threshold; B574-style local variant) -- W8 ONLY switches to consume the tighter signal; other two cpr_narrow consumers (strat_cpr_narrow_momentum + strat_cpr_narrow_momentum_short) retain 0.15 pending their own walks; (2) dropped rsi_14>50/<50 (jointly closes S4-W8-RSI-NOOP-GATE). Post-B654 gate set: 4 distinct gates per direction (cpr_narrow_tight + above_cpr/below_cpr + above_avwap_50low/below_avwap_50low + price_above_ema_200/below_ema_200). Pre-B654 87.3% cpr_narrow True rate -> expected ~25% with tight 0.05 threshold; fire-count drop ~70% expected (measurement re-run in flight). |
| S5-B417-LONG-ONLY-CUBE | `b417-long-only-cube-direction-disagg-review` | **2 LONG-only B417-cube-derived STRATEGY_REGIME_AFFINITY entries not in B617 dual-audit scope** per `R5_VALIDATION_MANIFEST.md` M8. B617 family audit was scoped to dual `_strat3` strategies; B417 cube also produced single-direction entries for LONG-only strategies that were NOT audited because the dual-direction-blocking signature doesn't apply: `institutional_buy_momentum_long` ({bull}), `institutional_cluster_long` ({bear}). Each may still benefit from direction-disaggregated review (e.g., is `institutional_cluster_long` really better in bear than bull?), but the family-bug risk doesn't apply. LOW priority. | DEFERRED-STAGE-5 | **B632 (2026-06-08 owner directive):** added to queue per R5 manifest M8. Post-R5, confirm cube Sharpe sign per regime; if any single-bucket entry shows the OTHER regime is profitable too, expand the entry. LOW priority - both entries are LONG-only so no SHORT-direction was blocked. |
| S5-REGIME-AFFINITY-21-DEFERRED | `regime-affinity-21-deferred-dual-entries` | **All 21 deferred STRATEGY_REGIME_AFFINITY dual `_strat3` entries** (B617 family audit + B623 direction-disaggregated audit). R5 cube must emit direction-aware per-(strategy, direction, regime) Sharpe + n; per-entry verdicts confirm or contradict B623 KEEP/REMOVE/INSUFFICIENT classifications. **Per-entry detail + B623 PnL comparison + R5 success criteria in `R5_VALIDATION_MANIFEST.md` M1.** Per owner directive 2026-06-08: defer all 21 till R5; no action on STRATEGY_REGIME_AFFINITY map until R5 closes this ticket. Affected entries: adx_initiation, avwap_252_breakout, awesome_oscillator, bollinger_tight, camarilla_s3_bounce, cmf_flip, force_index_breakout, macd_fast_crossover, morning_star, parabolic_sar_flip, pivot_s1_bounce, pivot_s2_bounce, ppo_crossover, prev_day_high_break, prev_day_low_bounce, smc_bos_retest_entry, smc_inverse_fvg, supertrend_macd, tema_dema, ultimate_oscillator, williams_stoch_dual. | DEFERRED-STAGE-5 | **B617 (2026-06-07):** family audit kept these 21 deferred for direction-disaggregated cube validation. **B623 (2026-06-08):** post-filter audit on existing 29360-trade cube data produced KEEP/REMOVE_OK/INSUFFICIENT/NO_TRADES verdicts per entry; 4 REMOVE_OK candidates (camarilla_s3_bounce, force_index_breakout, prev_day_low_bounce, ultimate_oscillator) gain or do not lose PnL under B291 direction-aware default. **B624 (2026-06-08 owner directive):** all 21 explicitly DEFERRED-STAGE-5; no map changes until R5 produces fresh direction-aware data. See `R5_VALIDATION_MANIFEST.md` M1 for per-entry detail. |
| PYRAMID-CLEANUP-ENV | `pyramid-environmental-failures-remaining-post-B622` | **14 remaining pyramid failures after B622 count + fixture drift cleanup.** Categories: (1) registry-drift tests where the in-file CLASSIFICATION / EXPECTED_STATUS dicts haven't been updated as new scripts/bugs landed (B465 orphan scripts x2 -- need 8 new entries incl. B619 estimate_fire_count.py + B621 audit_fire_counts.py; B466 bug-engine-status x2 -- BUG-015 + BUG-022 status drift YES -> PARTIAL-ORPHAN); (2) data/PIT freshness tests (B473 congressional_alt x2 PIT future-quarter drop; B496 SEC EDGAR decoded cache absence; B533 AWS keys in drift audit x2; B537 EMA panel match; test_data_integrity_2_ohlcv_freshness); (3) dashboard/preflight artifact tests (B568 preflight rerun status; B569 dashboard rows_lite); (4) engine parity test_engine_optimization_parity + test_integration x2 (bug_30 circuit breakers + bug_232 intraday-extreme). | DEFERRED-POST-B622 | **B622 (2026-06-08):** scoped to count + fixture drift only (26 fixes covering ~62pct of pre-B622 failures). The remaining 14 each need individual per-item investigation: registry updates need careful manual review of new vs accepted-deferred entries; data tests need cache regeneration or skip conditions; dashboard tests need artifact regen; engine parity needs golden-fixture investigation. Defer to a focused tooling/registry batch. |
| S5-ATR-SENSITIVITY | `atr-tolerance-sensitivity-scan` | **1.5xATR(14) retest-tolerance sensitivity scan across the retest family** per CHECKLIST #105 (p) codified B619 + external-AI critique standing concern across 4 walks. Replay each of the 10 retest-family strategies at multi-ATR tolerance settings (0.5x / 1.0x / 1.5x / 2.0x / 2.5x) across the cube + select the per-strategy tolerance that maximizes a joint risk-adjusted-return + fire-count criterion. Affected producers: `compute_break_retest_signals` (DC20 retest), `compute_flag_break_retest_signals` (flag retest), pivot/level proximity producers. Affected strategies: strat_break_retest_volume, strat_break_retest_confluence, strat_flag_bull_retest_long, strat_flag_bear_retest_short, strat_52wh_break_retest, strat_52wl_break_retest_short, strat_r1_break_retest, strat_donchian_breakout_retest_long, strat_donchian_breakdown_retest_short, strat_volume_spike_breakout_retest. | DEFERRED-STAGE-5 | **B619 (2026-06-08):** codified as Stage 5 cube task per CHECKLIST (p); walks STOP attempting hand-tuning of the 1.5x default. Requires multi-ATR cube replay infrastructure (does NOT exist yet) -- script that takes a tolerance multiplier parameter and re-runs the affected producers + downstream strategies. Success criteria: per-strategy ATR multiplier where (Sharpe + fires >= 30/yr) is jointly maximized; if no setting passes, mark strategy as exploratory + flag for re-thesis. See `R5_VALIDATION_MANIFEST.md` M2 for cross-reference. |
| S4-QUIET-AUDIT | `quiet-strategy-producer-bug-audit` | **Systematic QUIET-strategy producer-bug audit** per owner directive 2026-06-04. | RESOLVED-SWEEP-COMPLETE | **B582:** found year_high producer bug (52w family, 3 strats). **B584:** found Donchian rolling-window bug (6 strats). **B585:** sweep of all rolling-window producers in technical.py CLEAN -- 11 other patterns canonical (Williams %R / Stochastic / Bollinger / MFI / OBV / EMA / CMF / Ichimoku / Chandelier / Squeeze Momentum); no more lurking bugs of this pattern. Audit COMPLETE. Note: bug-fix is NOT S4 review per B585 correction; affected strategies still need full 7-step walks via S4-WALK. |
| S4-WALK | `stage-4-per-strategy-walks` | **Stage 4 per-strategy 7-step walks** per `feedback_per_strategy_deep_dive_stage4`. R5 cube paused until all 216 walks complete per `feedback_r5_paused_pending_stage4_completion`. | IN_PROGRESS | **B586:** 52w_high_breakout walked + 4 changes shipped (vol 1.7x / sector filter / 2 pullback variants); +2 strategies (213->215). **B587:** combined walk of inverse 52w_low_breakdown + 2 pullback variants (same B586 changes mirrored). **B588:** full 7-step exposition on pullback pair + 52w_high_breakout_with_smart_money_long + added missing inverse 52w_low_breakdown_with_smart_money_short (215->216). **S4 progress 2026-06-04:** 25 / 216 (12pct) REVIEWED, 191 / 216 (88pct) PENDING. **Next batch (B589+):** combined walks on donchian_10_breakout (dual) + donchian_breakdown_short + donchian_breakout_with_smart_money_long (B584 producer-fix cluster). |
| S4-WALK | `stage-4-per-strategy-deep-dive-walks` | **Stage 4 walks** — per-strategy 7-step deep-dive for all 207 strategies surfaced in approvals.json (122 fired + 83 quiet + 2 ghosts). Cluster-by-cluster execution; one strategy at a time per owner directive 2026-06-04. | IN_PROGRESS | **B572-B574 candle cluster started:** doji_at_support / doji_at_resistance_short walked + Class 2 tolerance loosen Implemented narrowly via `_wide` flags. **B575 STRATEGY_ROSTER.md** auto-gen + SIGNAL_GLOSSARY shipped. **B576 drift closure** + CHECKLIST #104 local-default rule. **Next:** `three_white_soldiers` (owner question deferred). After: morning_star (dual `_strat3`), bullish_engulfing_support (dual), shooting_star_short, evening_star_short. Then move to next cluster (TBD). Quiet strategies (83) get individual walks after fired-cluster pass. |
| S4-WIRE-NSS | `wire-news-sentiment-shift-short` | **Wire `strat_news_sentiment_shift_short` in screener.py** per B571 Class 7 Approved + `feedback_wire_new_strategies_on_the_spot`. Producer signals exist (`news_sentiment_shift`, `news_article_count`). Tetlock 2007 RFS asymmetry — short variant is theoretically higher-conviction than the existing long. Closes 2nd ghost in approvals.json. | PENDING | Approved in B571; deferred when owner said "1 strategy at a time" but Class 7 wiring is mechanical (~10 lines). Bumps ALL_STRATEGIES 205 -> 206. Pin doc-count drift tests. |
| S4-REGIME | `regime-affinity-investigation` | **Only 1 of 205 strategies has explicit `STRATEGY_REGIME_AFFINITY`** (`head_and_shoulders_bottom_long`). 204 fall through to "all regimes". Per-strategy R4 cube data should feed back into deployment-time affinity rules. | PENDING | Surfaced B577 from strategy roster audit. Likely needs per-strategy 5-Gate verdict per (strategy × regime) cell → derive affinity = {regimes where PASSes}. Class 5 STRATEGY_REGIME_AFFINITY decisions per workflow line 337 are auto-Deferred during Phase 1A-β but the empirical input layer (per-regime verdicts) needs to be wired regardless. |
| S4-DOC-CONSOLIDATE | `strategy-doc-consolidation` | **3 overlapping strategy docs**: STRATEGY_REGISTER.md (hand-curated, stale, 186 count), STRATEGY_ROSTER_FULL.md (hand-curated, stale, Layer 1-4), STRATEGY_ROSTER.md (auto-gen B575, live, 205). ~80% content overlap; drift independently. | RESOLVED-PARTIAL | **B578 (2026-06-04):** STRATEGY_ROSTER_FULL.md archived; Projected Strategies section added to STRATEGY_ROSTER.md sourced from STRATEGY_REGISTER.md Layer 4. STRATEGY_REGISTER.md kept (canonical DEC registry). |
| S4-LAYER-2D-ICT | `layer-2d-ict-inline-spec` | **Layer 2D ICT methodology intake** (Option A 2026-06-04). | RESOLVED-OWNER-CONCLUDED-B582 | **B582 (2026-06-04 owner directive):** "No more ICT patterns. resolve ICT status." Final Layer 2D coverage: 8 strategies wired across B580+B581 (Turtle Soup long+short, Judas Swing long+short, MMBM/MMSM, Week Opening Gap up+down). 2 of original 6 owner-spec strategies remain BLOCKED with no path forward in current architecture (Silver Bullet, SMT Divergence -- see linked rows). Inline-spec protocol stays available if owner re-opens. |
| S4-ICT-SILVER-BULLET | `ict-silver-bullet-blocked-intraday` | **Silver Bullet** (1-hour high-volatility windows e.g. 10-11 AM NY). | RESOLVED-PERMANENTLY-BLOCKED-B582 | **B582 (2026-06-04 owner "resolve ICT status"):** permanently blocked. Daily-bar cube cannot represent intraday windows; CLAUDE.md "Intraday trading: completely separate future project — out of scope" gates indefinitely. Will not reopen unless intraday pipeline ships separately. |
| S4-ICT-MMBM | `ict-mmbm-pending-producer` | **Market Maker Buy/Sell Model** (Accumulation -> Manipulation -> Distribution daily cycle). | RESOLVED-IMPLEMENTED-B581 | **B581 (2026-06-04):** wired `strat_mmbm_long` + `strat_mmsm_short` per owner directive "implement now". NEW producer `compute_po3_signals()` in `backtest/signals/ict_producers.py` detects 3-phase pattern on daily bars (accumulation_window=5, tight_range_threshold=5%). ALL_STRATEGIES 207 -> 213 (+2). 13 unit tests in B581 batch. |
| S4-ICT-WEEK-OPEN-GAP | `ict-week-open-gap-pending-producer` | **Week Opening Gap fill** (Sunday open gap return). | RESOLVED-IMPLEMENTED-B581 | **B581 (2026-06-04):** wired `strat_week_opening_gap_fill_down` (short upside gap) + `strat_week_opening_gap_fill_up` (long downside gap). NEW producer `compute_week_opening_gap_signals()` detects Monday-vs-Friday gaps (daily-bar proxy for ICT Sunday gap). Threshold 1.5%. Same B581 batch. |
| S4-ICT-JUDAS-SWING | `ict-judas-swing-variant` | **Judas Swing / Stop Hunt variant** (false range-break + deep return to pivot midpoint). | RESOLVED-IMPLEMENTED-B581 | **B581 (2026-06-04):** wired `strat_judas_swing_long` + `strat_judas_swing_short`. Owner approved + directed wire same turn. Distinct from `smc_liquidity_sweep_reversal` (requires CHoCH/BOS) and `turtle_soup_long/_short` (close back above prior_low). Same B581 batch. |
| S4-ICT-SMT-DIVERGENCE | `ict-smt-divergence-not-in-scope` | **SMT Divergence** (correlated-asset divergence). | RESOLVED-NOT-IN-SCOPE-B582 | **B582 (2026-06-04 owner directive):** "Build SMT Divergence producer - Not in scope." Permanently descoped from project. Will not be built unless owner re-opens. |
| DET1 | `engine-cross-platform-determinism` | **Engine produces ENTIRELY DIFFERENT trades on Windows vs Linux** | RESOLVED-PIN-MAXED-OUT-RSI14-PLATFORM-FP-ACCEPTED | **Batch 531 (2026-05-31) DET1 follow-on COMPLETED:** workflow regenerated Linux baseline under pinned `pandas==3.0.2` + `numpy==2.4.4` (auto-commit `c1e86edec`). Cross-platform diff verified locally: **8 of 9 indicators bit-identical; `rsi_14` STILL diverges sub-epsilon even with bit-identical library versions on both platforms**. Root cause CONFIRMED: rsi_14 divergence is platform FP-math (Linux glibc vs Windows MSVC, BLAS variant -- OpenBLAS on Linux vs Intel MKL on Windows), NOT library-version. Pinning libs alone CANNOT fix it. **ACCEPTED_DIVERGENT = {"rsi_14"} stays as the correct call**. Net DET1 state: (a) Windows-local + CI + new-laptop all install bit-identical libs (DET1 at lib-pin level closed); (b) 8 of 9 indicators bit-identical cross-platform; (c) rsi_14 sub-epsilon platform-FP drift is known/accepted limit; (d) `test_batch498_cross_platform_diff_when_both_baselines_present` activates with both fingerprints present and gates against NEW divergent indicators beyond rsi_14. **Batch 525 (2026-05-31) laptop-portable lock LANDED:** rewrote `requirements.txt` -- every dep hard-pinned (`==`) at Windows-baseline versions (pandas==3.0.2, numpy==2.4.4, pyarrow==24.0.0, scipy==1.17.1, numba==0.65.1, statsmodels==0.14.6, scikit-learn==1.8.0, xgboost==3.2.0, filelock==3.29.0, pytest==9.0.3 + 8 more = 18 strict pins). New `scripts/verify_environment.py` runs (a) installed-version vs requirements.txt pin diff + (b) cross-platform fingerprint diff vs committed baseline; emits exit 0/1/2. 7-test regression guard `test_batch525_requirements_lock.py` bans loose pins, asserts DET1-critical libs pinned, asserts pandas/numpy match Windows baseline + Batch 520 CI workflow pin. **Batch 520 (2026-05-31) CI production fix LANDED:** pinned `pandas==3.0.2` + `numpy==2.4.4` across `test-pyramid.yml` (matrix + informational), `det1-platform-determinism.yml` (Linux baseline generator), `phase_1a_beta.yml` (prepare + batch + merge). 5-test regression guard. **Together (520 + 525) close DET1 at the root** -- CI + local + new-laptop all install bit-identical libs. ACCEPTED_DIVERGENT allow-list retained as belt-and-braces until Linux baseline regen confirms rsi_14 fingerprint now matches under pin. **Follow-on:** re-trigger DET1 workflow to regenerate Linux baseline + remove rsi_14 from ACCEPTED_DIVERGENT. **Batch 518/518b:** rsi_14 SOLE divergent indicator; root cause pandas 3.0.2 vs 3.0.3 NaN handling. **Batch 515:** root cause. **Batch 508:** workflow_dispatch. **Batch 498:** original harness. |
| P10 | `producer-polygon-news-sentiment` | **Signal producer (Batch 467)**: wire Polygon news to screener | RESOLVED | **Batch 467 (2026-05-29):** extended `news_sentiment.py` with `news_sentiment_5d` (recency-weighted), `news_sentiment_30d`, `news_volume_zscore_5d`, `news_count_5d`. Added `compute_simple_returns` to `technical.py` (pct_change_5d/10d/20d). Added 2 new strategies `strat_news_momentum_long` (sentiment≥0.5 + vol_z≥1.5 + dc20_breakout) + `strat_news_reversal_short` (sentiment≥0.7 + pct_change_5d>0.10). Registered in ALL_STRATEGIES (186→188). 13 tests pass. Wired in screener (Batch 458 silent-failure logger). |
| P11 | `producer-cftc-cot-positioning` | **Signal producer**: CFTC COT for macro overlay | RESOLVED | **Batch 472 (2026-05-29):** new module `backtest/signals/cot_positioning.py` reads `cot_emini_sp500/ndx/rut/dow + cot_dxy/gold/copper/...parquet`. Emits per-series `cot_<x>_commercials_net_pct` + `cot_<x>_commercials_pctile_3y` + `cot_<x>_mmoney_pctile_3y` over rolling 156-week (3y) baseline. Resolves both commodity (prod_merc_*) and financial (dealer_*/lev_money_*) column conventions. 6 tests pass. Universe-wide injection via `get_all_cot_signals(as_of)` wired in screener. |
| P12 | `producer-apewisdom-stocktwits-retail-sentiment` | **Signal producer**: retail social sentiment | RESOLVED-SKIPPED-OWNER-DECISION | **Owner decision 2026-05-31:** SKIP entirely. apewisdom data won't backfill historically (cache has 2 snapshot dates 2026-05-05/06; backtest needs 2022-2026; >99% of (ticker, as_of) pairs would emit empty signals). Producer not shipped; no strategies depend on retail social sentiment. Documented gap acknowledged; no re-prioritization planned. **Batch 467 schema verification:** confirmed data sparsity blocked the wire-in. Owner explicitly chose Option (b) "skip strategy line items" over backfill (cost) or defer (open obligation). |
| P13 | `producer-pytrends-search-volume` | **Signal producer**: Google search volume | RESOLVED | **Batch 471 (2026-05-29):** new module `backtest/signals/search_volume.py` reads weekly `pytrends/<TICKER>.parquet`. Emits `search_volume_index_recent` (last 0..100 reading) + `search_volume_zscore_30d` (last 4 weeks vs trailing 26-week baseline, Da-Engelberg-Gao 2011 style) + `search_volume_observations`. Coverage verified: 5 years across 1417 tickers (CHECKLIST #99 schema check). 6 tests pass. Wired in screener. |
| P14 | `producer-insider-quality-differentiation` | **Signal producer**: extend `insider_signal()` | RESOLVED | **Batch 469 (2026-05-29):** extended `smart_money.insider_signal()` with `cfo_buy` (regex CFO\|Chief Financial), `director_only_buy` (isDirector=True AND isOfficer=False), `large_dollar_buy` (Shares*PricePerShare > $1M), `concentrated_sell` (Shares / total_holdings_pre_sale > 50%). Composite signal updated: cluster_sell OR concentrated_sell -> top bearish; cfo/large/cluster_buy -> "buy"; director_only -> "weak_buy". 7 tests pass. NO SEC EDGAR involvement -- Quiver-only per Batch 453 audit correction. |
| P17a | `prerequisite-sec-edgar-xml-extractor` | **Pre-req for P17b/c/d/e** | RESOLVED-100PCT-COMPLETE | **Scoped extraction COMPLETED 2026-06-01:** 136,189 / 136,209 filings decoded (**99.985% success, 20 errors**) across 1,722 tickers × 3 forms (SC_13D=623, SC_13G=1,616, 8_K=1,543 = 3,782 parquets). Batch 526 validator post-completion: 5/6 PASS (Gate 1 SC_13D coverage 36% is rare-event sparsity, not failure — most universe tickers genuinely have 0 SC 13D in 6yr window; activist filings are 50-200/yr universe-wide). SC_13G 93.8% / 8_K 89.9%. Status counts: 99.79-99.99% `ok`. Spot check (8-K 2024): AAPL=9, MSFT=8, AMZN=8, GOOGL=12, META=6. Wire-in proven on real data: ABSI 2021-08-03 returns `sc_13d_filed_within_30d=True` through `compute_sec_edgar_signals` -> screen_instrument -> strategy. **P17 sleeves (P17b/c/d/e) now production-ready with full signal density.** **Scoped run started 2026-05-31**: 1,100 decoded parquets (SC_13D=181, SC_13G=469, 8_K=450) of ~5,000 target (~22% complete; ETA 6-8h). On completion: (1) Batch 526 validator (`scripts/validate_sec_edgar_decoded_completeness.py`) runs 6 gates; if green, (2) auto-ship P17 sleeve wire-in batch per owner directive 2026-05-31: wire `compute_sec_edgar_signals` into `screener.screen_instrument`; register `activist_13d_long` + `m_and_a_target_long` in ALL_STRATEGIES; activate `tier_modifier_officer_change_5_02` in tier-assignment + `smart_money_score_modifier_13g` in smart_money_score. Same per-sleeve safety pattern as Batch 519 P15 sleeves. **Batch 514 (2026-05-31) Pilot LIVE RUN EXECUTED 100% success** per owner directive. UA hotfix shipped (Batch 509 UA "jeetmehta1991-stock-picks-app/1.0 (...@noreply...)" -> 403; corrected to "Stock Picks Research jeetmehta1991@gmail.com" -> 200 OK; SEC's standard `Name email@domain.com` format). Final results: **SC_13D 15/15 OK, SC_13G 642/642 OK, 8-K 493/493 OK -- 1150/1150 (100%)**. Decoded outputs land at `data_prefetch/sec_edgar_decoded/<form>/<TICKER>.parquet` -- gitignored (reproducible artifacts). Sample AAPL 8-K item codes extracted: 2.02 (earnings results), 5.02 (officer changes) -- semantic extraction verified end-to-end. **Next step (owner):** scale up to full universe extraction (~300k filings; same rate-limit -> ~10 hours; or run on GitHub Actions). **Batch 509 (2026-05-31):** pilot extractor + dry-run verification. **Batch 496 (2026-05-30):** original extractor scaffold + 21 tests. |
| P17b | `signal-sc-13d-activist-sleeve` | **Ship 1: SC 13D activist filing → sleeve overlay** | RESOLVED-REGISTERED-AND-WIRED | **Batch 531 (2026-05-31) ACTIVATED** per owner directive "wire in activate truly pending items": `activist_13d_long` registered in `ALL_STRATEGIES` (203→204; live count 204 total / 203 active). Producer bundle `compute_sec_edgar_signals` wired into `screener.screen_instrument` via silent-failure logger (mirrors P15/P16 convention). B522 NOT-REGISTERED guard test FLIPPED to REGISTERED assertion (`test_batch522_p17_strategies_ARE_registered_in_all_strategies`). Fires when `sc_13d_filed_within_30d=True` from SEC EDGAR decoded cache (currently ~22% populated via P17a scoped extraction; signal density will grow as extraction completes; sleeve emits trades on bars where activist filing landed in trailing 30d). **Batch 522 (2026-05-31) SCAFFOLD shipment** -- `strat_activist_13d_long` added to `screener.py` (NOT registered in ALL_STRATEGIES per owner-gate); fires `long` when `sc_13d_filed_within_30d=True` from `compute_sec_edgar_signals(ticker, as_of)`. Producer bundle `compute_sec_edgar_signals` added to `sec_edgar_extractor.py` (also NOT wired into `screener.screen_instrument`). 19 unit tests in `test_batch522_p17_sleeve_scaffolds.py` cover fire semantics + safety-default-non-firing when signal absent + NOT-REGISTERED guard. **Owner unlock to ship:** when P17a scoped extraction completes, owner approves a follow-on batch to (a) wire `compute_sec_edgar_signals` into `screen_instrument` and (b) register `activist_13d_long` in ALL_STRATEGIES. **Blocked-by:** P17a. **Why valuable:** Brav-Jiang-Partnoy-Thomas 2008 *Journal of Finance* +6.8% abnormal return in 30d window around 13D filing; Bebchuk-Brav-Jiang 2015 sustained +3-5pp/yr alpha for 5 years post-filing. Filers: Icahn, Ackman, Peltz, Elliott, ValueAct, Starboard. **NOT in Quiver** (sec13f is quarterly holdings with 45d lag, not activist intent). **Source after P17a:** `data_prefetch/sec_edgar_decoded/SC_13D/<ticker>.parquet` cols (expected post-extractor): `ticker, filing_date, filer_identity, percent_owned, item_4_purpose_text`. **Consumer:** new boolean `sc_13d_filed_within_30d` exposed by `sec_catalyst_signal()`; sleeve strategy variants `<base>_with_13d_activist` (e.g. `value_breakout_with_13d_activist`, `cup_handle_with_13d_activist`) compete in cube as independent cells. **Honest cost:** rare events (~50-200/yr universe-wide; ~5-10% of tickers/yr see any 13D activity) → likely fails DEC-426 n≥30 as standalone; sleeve overlay is the right pattern. **Smallest code, biggest per-event impact — ship first.** |
| P17c | `signal-8k-item-1-01-merger-target-strategy` | **Ship 2: 8-K Item 1.01 (material agreement) → `m_and_a_target_long`** | RESOLVED-REGISTERED-AND-WIRED | **Batch 531 (2026-05-31) ACTIVATED**: `m_and_a_target_long` registered in `ALL_STRATEGIES`. Producer wire-in shared with P17b. Fires when `8k_item_1_01_filed_within_30d=True` from `eight_k_item_filed_within_days(item_code="1.01", lookback_days=30)`. **Batch 522 (2026-05-31) SCAFFOLD shipment** -- `strat_m_and_a_target_long` added to `screener.py` (NOT registered); fires `long` when `8k_item_1_01_filed_within_30d=True` from `compute_sec_edgar_signals`. Item-code parsing already lands via Batch 509's `extract_8k_item_codes` (regex over EDGAR HTML "Item X.YY" anchors); `eight_k_item_filed_within_days` consumer matches the parsed comma-separated `item_codes` column. Same NOT-REGISTERED safety guard + same owner-unlock as P17b. **Blocked-by:** P17a + item-code text parser. **Why valuable:** Pawliczek-Skinner 2018 *Review of Accounting Studies* — Items 1.01 + 2.02 predict short-term returns (~2-3pp 10-day CAR). Item 1.01 is frequently the FIRST public disclosure that a company is being acquired or signed a major partnership — stock often gaps 10-30% on next bar. **NOT in Quiver.** **Source after P17a + item-code parser:** `data_prefetch/sec_edgar_decoded/8_K/<ticker>.parquet` cols (expected): `ticker, filing_date, item_code (1.01/2.01/2.02/5.02/...), item_body_text_summary`. **Consumer:** `8k_item_1_01_filed_within_30d` boolean → new standalone strategy `m_and_a_target_long` (enters next bar after 8-K Item 1.01 if no merger-already-announced exclusion). **Cost:** Item 1.01 frequency moderate (companies file ~1-3 per year doing material deals); high signal-to-noise. **Needs item-code body parsing** beyond the form-type extraction — `primary_doc` XBRL contains item codes in document body, not in filing-index. P17a extractor must include item-code field for 8-K specifically. |
| P17d | `signal-8k-item-5-02-officer-change-event-overlay` | **Ship 3: 8-K Item 5.02 → 7-day tier downgrade overlay** | RESOLVED-WIRED-INTO-TIER-ASSIGNMENT | **Batch 531 (2026-05-31) ACTIVATED**: `tier_modifier_officer_change_5_02` wired into `backtest/engine/backtest.py` POST `_assign_confidence_tier()` call. Downgrades preliminary tier by 1 slot when `8k_item_5_02_filed_within_7d=True`; floors at LOW; silent-failure guarded. B522 NOT-WIRED guard FLIPPED to WIRED assertion (`test_batch522_modifiers_ARE_wired_in_tier_and_score_logic`). **Batch 522 (2026-05-31) SCAFFOLD shipment** -- `tier_modifier_officer_change_5_02` added to new module `backtest/signals/sec_edgar_modifiers.py`; downgrades preliminary tier by 1 slot (LOW < MEDIUM < MEDIUM-HIGH < HIGH < VERY HIGH < EXCEPTIONAL lattice) when `8k_item_5_02_filed_within_7d=True`, floors at LOW. **NOT wired** into `_assign_confidence_tier` -- grep-style guard test asserts the symbol is absent from `screener.py`. Owner-unlock: approve a follow-on batch that wires the modifier into the tier_assignment call path. **Blocked-by:** P17a + item-code text parser (same as P17c). **Why valuable:** CEO/CFO departure carries high uncertainty premium; sudden CFO exits especially predictive of accounting issues. Often precedes downward earnings revision. Academic: prior Form 8-K studies cited above. **NOT in Quiver.** **Source after P17a:** same as P17c (8_K decoded cache). **Consumer:** `8k_item_5_02_filed_within_7d` boolean → event overlay in `_assign_confidence_tier` (downgrade preliminary tier by 1 slot for affected ticker during 7-day window). NOT a sleeve strategy or standalone; pure tier modifier. **Cost:** Item 5.02 frequency: ~1-2 per year per ticker; high coverage; moderate per-event signal. |
| P17e | `signal-sc-13g-passive-flow-overlay` | **Ship 4: SC 13G → smart-money score +1 modifier** | RESOLVED-WIRED-INTO-SMART-MONEY-SCORE | **Batch 531 (2026-05-31) ACTIVATED**: `smart_money_score_modifier_13g` wired into `backtest/data/smart_money.py:smart_money_score()` POST composite-band assignment. Adds +1 to score when `sc_13g_filed_within_30d=True`; recomputes composite band from new score so downstream tier-assignment sees boosted signal. Silent-failure guarded. **Batch 522 (2026-05-31) SCAFFOLD shipment** -- new helpers in `sec_edgar_extractor.py`: `sc_13g_filed_within_days(ticker, as_of, lookback_days)` mirrors the 13D helper; `compute_sec_edgar_signals` includes its 30d boolean in the producer bundle. `smart_money_score_modifier_13g(ticker, as_of, current_score)` in `sec_edgar_modifiers.py` adds +1 when triggered. **NOT wired** into `data/smart_money.py:smart_money_score` -- grep guard asserts the symbol is absent from `smart_money.py`. **Blocked-by:** P17a. **Why moderate value:** passive flow ≠ stock-picking edge; Vanguard/BlackRock crossing 5% predicts index reweighting + forced index-fund buying. Smart-passive concentration (Sequoia, Wellington, GMO) signals quality. **Best per-event signal is the 13D→13G TRANSITION** (activist gave up = sell) per Bebchuk-Brav-Jiang — but that requires joining 13D + 13G filings by filer identity, harder than single-form detection. **NOT in Quiver.** **Source after P17a:** `data_prefetch/sec_edgar_decoded/SC_13G/<ticker>.parquet` cols (expected): `ticker, filing_date, filer_identity, percent_owned`. **Consumer:** `sc_13g_filed_within_30d` boolean → +1 to `smart_money_score()`. LOW priority per recommendation — ship after P17b/c/d land and prove value. **Cost:** higher volume than 13D (500-2000/yr universe-wide); lower per-event signal. The 13D→13G transition variant (high-signal) is deferred until owner approves the extra extractor work. |
| P15 | `producer-finra-short-interest` | **NEW data source + producer**: FINRA short interest / days-to-cover | RESOLVED-SLEEVES-WIRED-AND-REGISTERED | **Batch 519 (2026-05-31) FULLY WIRED.** Sleeves registered: `strat_squeeze_setup_long` (high SI + DC20 breakout + volume; Cohen-Diether-Malloy 2007 squeeze) + `strat_short_borrow_trap_avoid` (avoid-direction when DTC > 5; borrow-constraint premium). `compute_short_interest_signals` wired into `screen_instrument` with silent-failure guard. ALL_STRATEGIES 200->202; 13 new tests. **Batch 516 (2026-05-31):** 1926 universe-ticker parquets cached. **Batch 513 (2026-05-31):** fetcher + 15 tests. **Batch 494 (2026-05-30):** producer + 11 tests. |
| P16 | `producer-quiver-alt-data-overlays` | **Signal producer**: 6 Quiver alt-data sub-feeds wired (housetrading + gov_contracts + lobbying + patentmomentum + offexchange + corporatedonors) | RESOLVED | **Batch 528 (2026-05-31) P16 COMPLETION:** added 3 remaining producers to `congressional_alt_data.py`: `compute_patentmomentum_signals` (Hirshleifer-Hsu-Li 2013 RFS; reads global `patentmomentum/global.parquet` 5.8M rows, emits `patent_momentum_recent` + `patent_momentum_90d_avg` + `patent_momentum_above_avg`); `compute_offexchange_signals` (Comerton-Forde-Putnins 2015 JFE; reads per-ticker `offexchange/<TICKER>.parquet`, emits `otc_short_ratio_recent` + `otc_volume_recent` + `dpi_recent` + `dpi_30d_avg` + `dpi_elevated`); `compute_corporatedonors_signals` (Akey 2015 RFS; reads global `corporatedonors/global.parquet` 25K rows, emits `corp_donations_1y` + `corp_donations_count_1y` + `corp_donations_unique_pacs`). All 3 wired in `screen_instrument` with `_log_silent_producer_failure` per Batch 458 convention. Module-level cache for the 2 global parquets (one read per screener-run, not per-ticker). 11 unit tests in `test_batch528_p16_completion.py` pin PIT cutoff, dict schema, silent failure on missing data, wire-in regression guard for all 6 P16 producers. Real-data smoke: AAPL/MSFT/XOM emit non-empty offexchange + patentmomentum signals. **Batch 480 (2026-05-30):** `compute_lobbying_signals` (Hill-Kelly-Lockhart 2014). **Batch 473 (2026-05-29):** original housetrading + gov_contracts. |
| SM1 | `smart-money-sleeve-strategies` | **Smart money expansion (Batch 450)**: 10 new sleeve strategies that gate entry on smart money signal | RESOLVED | **Batch 528 (2026-05-31) queue sync:** SM1 was already implemented in Batch 487 SM1 (2026-05-30) but queue note was stale. Live state today: `len(ALL_STRATEGIES) = 202`; 37 smart-money-related strategy keys (`insider_cluster_long`, `institutional_cluster_long`, `institutional_buy_momentum_long`, `institutional_distribution_short`, `institutional_oversold_long`, `institutional_breakout_confirmation_long`, `institutional_insider_combo_long`, `institutional_volume_confirmation_long`, `pead_with_insider_confirmation_long`, `insider_cluster_with_director_long` + 27 more). Originally targeted +10; cleared the target. R4 cube replay will rank sleeve cells vs base cells empirically. |
| SM2 | `exit-smart-money-reversal` | **Smart money expansion**: 26th exit method - exits on smart money signal flip during hold | RESOLVED | **Batch 528 (2026-05-31) queue sync:** SM2 was already implemented in Batch 487 SM2 (2026-05-30) but queue note was stale. Live state today: `len(EXIT_STRATEGIES) = 26` including `smart_money_reversal`. Cube R4 replay will rank vs `regime_flip` / `atr_trail_1x` / etc when R4 unpauses. |
| M1 | `missing-inter-strategy-correlation` | **Missing analysis**: pairwise correlation matrix + cluster detection | RESOLVED | **Batch 475 (2026-05-29):** new module `backtest/results/strategy_correlation.py` with `compute_strategy_correlation_matrix(trade_log)` (per-strategy daily-entry-count Pearson r) + `cluster_correlated_strategies(matrix, threshold=0.8)` (union-find clusters) + `redundant_strategy_pairs(matrix, threshold)`. 8 tests pass (anti-aligned negative; perfectly-aligned 1.0; clusters group redundant pairs; empty handles cleanly). M9 consumes this for effective_strategy_count. |
| M2 | `missing-capacity-analysis` | **Missing analysis**: per-cell capacity flag | RESOLVED | **Batch 476 (2026-05-29):** new module `backtest/results/capacity_analysis.py` with `compute_cell_capacity(adv_at_entry, position_dollars)` + `compute_cell_capacity_from_trade_log(...)`. Emits median + max `size_pct_of_adv` + `capacity_concern_flag` (Almgren-Chriss 2001 0.1% ADV threshold). 8 tests pass. **Batch 489 wire-in (2026-05-30):** `cube_populator.compute_cell_metrics` emits `median_size_pct_of_adv` / `max_size_pct_of_adv` / `capacity_concern_flag` when trade_log carries `adv_at_entry` + `position_dollars`; gracefully omits otherwise. 14 wire-in tests pass in `test_batch489_m_wire_ins.py`. |
| M3 | `missing-random-walk-adversarial-baseline` | **Missing analysis**: never verified strict 5-Gate produces ~0 PASS cells on noise | RESOLVED | **Batch 462 (2026-05-29) closed via AU6 semantic-integration framework:** `test_semantic_integration_pyramid.py::test_random_walk_zero_5gate_passes` feeds 40 trials of synthetic random-walk pnl% (mean=0) through `_dec426_verdict` + asserts ZERO strict 5-Gate passes. Paired with positive-control test asserting synthetic strong-edge DOES pass (two-sided check). **Batch 491 (2026-05-30) paperwork:** queue row was stale; M3 already RESOLVED by AU6. |
| M4 | `missing-final-oos-holdout` | **Missing analysis**: final OOS holdout guard | RESOLVED | **Batch 477 (2026-05-29):** new module `backtest/util/holdout_guard.py` defines `FINAL_OOS_HOLDOUT_START=2026-01-01` / `FINAL_OOS_HOLDOUT_END=2026-06-30` constants + `is_in_holdout(d)` predicate + `assert_no_holdout_intrusion(dates, caller)` raising helper + `HoldoutUnlock(reason)` context manager (audit-logged). Thread-local reentrant lock. 10 tests pass. Engine + optimizer wire-in is the follow-on (call `assert_no_holdout_intrusion` from cube/walk-forward entry points). |
| M5 | `missing-polygon-news-sentiment` | **Missing signal consumption**: ~1.05M Polygon news articles cached, no producer feeds the screener | RESOLVED-CROSS-REF-P10-B546-B552 | **2026-06-02 re-audit**: M5 IS already resolved by chain P10 (B467 producer wiring) + B546 (date pre-convert) + B552 (per-article score pre-compute + _score_window vectorize). Producer `compute_news_sentiment_signals` emits 13 keys (news_count_7d/article_count/sentiment_score/sentiment_mean/bullish_pct/bearish_pct/sentiment_shift/prior_article_count/uses_polygon_score/sentiment_5d/sentiment_30d/volume_zscore_5d/count_5d). 2 strategies consume it at entry: `strat_news_sentiment_long` (screener.py:3386) + `strat_news_reversal_short` (Batch 467 P10). Empirical: AAPL 2024-06-14 110 articles, sentiment_score 0.4121. Queue note was stale; this row was never updated post-P10/B467/B546/B552. |
| M6 | `missing-earnings-surprise-magnitude` | **Earnings surprise producer + PEAD-extension sleeve** | RESOLVED-PATH-2-SLEEVES-REGISTERED | **Batch 507 (2026-05-31) Path-2 SLEEVES REGISTERED** per owner directive. ALL_STRATEGIES count 198 -> 200. `strat_pead_long_high_yoy_growth_only` (fires on within_pead_window AND yoy_surprise_high) + `strat_pead_short_negative_yoy_growth` (symmetric short) added to screener.py + registered in ALL_STRATEGIES dict. `compute_yoy_surprise_signal` wired into `screen_instrument` signals dict alongside `compute_pead_signals` with silent-failure guard. Doc-sync: CLAUDE.md strategy count 198->200, drift_audit_live_values.json strategy_total 198->200 + strategy_active 197->199 + cube_cells_active 5122->5174. Strategy-count assertions updated in test_unit.py + test_silent_gap_pyramid.py + test_batch487_sm1_sm2.py + test_batch503_m6_path2_yoy_surprise.py (sleeves-registered pin flipped). **Batch 503 (2026-05-31):** original Path-2 producer module + 11 tests. **Batch 493:** data-gap pin tests for calendar_earnings.parquet. Path-1 (paid Finnhub re-prefetch) and Path-3 (defer) remain outstanding alternatives. |
| M7 | `missing-short-interest-coverage` | **Missing signal**: no days-to-cover / short interest feed | RESOLVED-CROSS-REF-P15-B519 | **2026-06-02 re-audit**: M7 IS already resolved by P15 (B494/B513/B516/B519). FINRA short interest prefetched to 1,926 universe-ticker parquets (B516). Producer `compute_short_interest_signals` wired into `screen_instrument` with silent-failure guard (B519). Sleeves registered: `strat_squeeze_setup_long` (screener.py:2165; high SI + DC20 breakout + volume; Cohen-Diether-Malloy 2007 squeeze) + `strat_short_borrow_trap_avoid` (screener.py:2249; days_to_cover>5 avoid signal). 13 wire-in tests pass (B519). Queue note was stale; this row was never updated post-P15. |
| M8 | `missing-dynamic-strategy-retirement` | **Missing decision rule**: `project_no_apriori_strategy_pruning` says empirical only — but no trigger exists for empirical retirement | DEFERRED | **Owner Batch 450 directive C**: deferred post-1A-β / pre-next-phase. Decision-quality work — relevant when transitioning out of cube-optimization mode into deployment-prep. | The standing rule prevents A-PRIORI pruning. It doesn't define WHEN empirical retirement fires. Currently 25 all-negative-Sharpe strategies sit on the queue (#11) waiting for owner decision because there's no rule. **Action:** propose criterion (e.g., "rolling 12-month Sharpe < 0 across 2 consecutive months AND n ≥ 50" → retire) + dynamic check in walk-forward output. Closes queue item #11 with a rule instead of a one-off decision. |
| M9 | `missing-correlation-aware-confluence` | **Missing decision rule**: effective_strategy_count via correlation clusters | RESOLVED | **Batch 478 (2026-05-29):** new module `backtest/engine/correlation_aware_count.py` exposes `build_strategy_cluster_lookup(corr_matrix, threshold)` + `effective_strategy_count(firing_strategies, lookup)`. Singletons fallback to per-strategy id so un-clustered names still count. 6 tests pass (e2e from synthetic trade log -> correlation matrix -> lookup -> reduced count). **Batch 489 wire-in (2026-05-30):** `BacktestEngine.__init__` adds `self._strategy_cluster_lookup` (empty default = back-compat raw count) + `load_strategy_cluster_lookup(json_path)` loader. `_assign_confidence_tier(..., firing_strategies=None)` now collapses raw strategy count to effective cluster count via `correlation_aware_count.effective_strategy_count` when lookup is populated. Callsite at backtest.py:1528-1538 passes firing strategy names. 14 wire-in tests pass in `test_batch489_m_wire_ins.py`. |
| M10 | `missing-realistic-slippage-model` | **Missing analysis**: `_cost_sensitivity_sharpe` uses flat bps haircut — not ADV-dependent | DEFERRED | **Owner Batch 450 directive C**: deferred post-1A-β / pre-next-phase. Decision-quality work — relevant for deployment realism, not cube-best-cells identification. | Real slippage = f(order_size / ADV, spread, volatility). Flat 5/10/20 bps misrepresents both extremes. **Action:** extend `_cost_sensitivity_sharpe` to take ADV + spread + position size; produce realistic slippage haircut per trade. Then re-run the cost-sensitivity Sharpe with realistic model. |
| M11 | `missing-pnl-concentration-hhi-per-cell` | **Missing analysis**: PnL concentration metric per cell | RESOLVED | **Batch 474 (2026-05-29):** new module `backtest/results/pnl_concentration.py` with `compute_pnl_concentration(pnls)` -> top1 / top5 / HHI shares + `compute_pnl_concentration_from_trade_log(...)` per-cell. 11 tests pass (uniform -> HHI=1/n; single trade -> HHI=1; outlier dominates -> top1>95%; abs-value used). **Batch 489 wire-in (2026-05-30):** `cube_populator.compute_cell_metrics` now emits `pnl_concentration_top1_pct`, `pnl_concentration_top5_pct`, `pnl_hhi` per cell. 14 wire-in tests pass in `test_batch489_m_wire_ins.py`. |
| 0a | `pattern-3-rr-vs-profit-factor` | **Pattern 3 audit**: 5-Gate `rr_>=_2.0` uses `profit_factor` as proxy for R:R; mathematically different | RESOLVED-PATH-2-SWAP-SHIPPED | **Batch 506 (2026-05-31) Path-2 SWAP shipped** per owner directive: `_dec426_verdict.enforced_gates` is NOW `["n_>=_30", "p_<_0.05", "t_>=_3.4", "rr_actual_>=_2.0"]`. Actual R:R = avg_win / abs(avg_loss) drives verdict; `pf_>=_2.0` stays in gates dict as informational. Cells matching Batch 492 counter-examples (WR=60% PF=2.0 R:R=1.33; 90% WR PF=4.5 R:R=0.5) now FAIL verdict where they previously PASSED. 7 new tests in `test_batch506_0a_path2_enforce_rr_actual.py` + Batch 492/502/457 tests updated. Expected R3+ strict-pass count reduction; surfaces in next cube run. **History:** Batch 492 documented PF vs R:R counter-examples; Batch 502 Path-1 renamed dict key + shipped informational rr_actual; Batch 506 Path-2 swapped enforcement. |
| 0b | `pattern-3-bonferroni-denominator` | **Pattern 3 audit**: `_dec426_verdict(m_total_candidates=1)` default — Bonferroni M may be 1 (no correction) at runtime | RESOLVED | **Batch 462 (AU6) closed via `test_optimizer_main_bonferroni_denominator_runtime` in `test_semantic_integration_pyramid.py`:** asserts production callsite passes `M = max(len(fired) * 9, 1)` + `m_total_candidates=M` so runtime M > 1000. **Batch 495 (2026-05-30) paperwork close:** queue row was stale PENDING. Coverage regression-guard test added in `test_batch495_pattern3_closures.py::test_batch495_0b_bonferroni_coverage_in_au6_pyramid` -- a future refactor that removes AU6's test surfaces here. |
| 0c | `pattern-3-walk-forward-chronological-split` | **Pattern 3 audit**: verify `walk_forward_batch414_cells.py` IS/OOS folds actually respect time-ordering | RESOLVED | **Batch 462 (AU6) closed via `test_walk_forward_folds_chronological_disjoint` in `test_semantic_integration_pyramid.py`:** asserts per-fold `IS_end == OOS_start` (chronological) + cross-fold OOS year-sets disjoint + DEC-505 1-year span. **Batch 495 (2026-05-30) paperwork close:** queue row was stale PENDING. Coverage regression-guard test added in `test_batch495_pattern3_closures.py::test_batch495_0c_walk_forward_chronological_coverage_in_au6_pyramid`. |
| 0d | `pattern-3-liquidity-floor-actual-enforcement` | **Pattern 3 audit**: DEC-321/366 ADV floors exist in config; verify engine actually filters candidates by them | RESOLVED | **Batch 491 (2026-05-30):** 5 new tests in `test_batch491_audit_pattern3_closures.py` replicate the engine gate at `backtest.py:282-286` against synthetic OHLCV: (a) volume = floor - 1 rejected, (b) volume = floor passes (>= boundary), (c) price < min_price rejected even with high volume, (d) config values pinned (500k volume / $5 price / $100M cap), (e) NaN-volume documented-current-behavior (silently passes today; assertion flips when engine fail-closes on NaN). **Documented gap:** NaN avg_vol currently bypasses the gate -- not fixed here (out of scope; surfaces in next bug queue row if observed). |
| 0e | `pattern-3-survivorship-haircut-consumption` | **Pattern 3 audit**: `trade_log.survivorship_haircut` column exists; is it consumed by ANY downstream metric? | DEFERRED | **Owner Batch 450 directive C**: deferred post-1A-β / pre-next-phase. | **Audit finding 2026-05-29:** trade_log emits the column. metrics.py has no obvious `survivorship_haircut` consumer. If 0 rows trigger the haircut (e.g., because delisted-name detection silently returns False), the column is dead data. **Action:** count `survivorship_haircut != 0` rows in current cube; if 0, the haircut feature is non-functional; if non-zero, verify downstream Sharpe / WR computations actually adjust for it. |
| 0f | `pattern-3-regime-classifier-correctness` | **Pattern 3 audit**: regime classifier returns a string per trade; is the LABEL correct? | RESOLVED | **Batch 491 (2026-05-30):** 18 new tests in `test_batch491_audit_pattern3_closures.py` pin `classify_regime` against 13 parametrized (vix, above_200ema, bear_score, expected_label) tuples covering every branch of the Batch 288/292 ladder: crisis (VIX>=40), bear via SPY-only gate, bear via composite score, bull (VIX<20 + above_200ema + score<2), neutral. Plus pinned-history tests: COVID-2020 VIX=82 + below 200 EMA -> crisis; 2023 bull VIX=13.5 + above 200 EMA -> bull; missing VIX -> 'unknown' (DEC-316/BUG-225). Sweep test asserts output always in {bull, neutral, bear, crisis, unknown} legal-label set. |
| 0g | `pattern-3-cost-sensitivity-sharpe-consumption` | **Pattern 3 audit**: metrics.py computes Sharpe @ 0/5/10/20 bps; is any decision driven by these? | DEFERRED | **Owner Batch 450 directive C**: deferred post-1A-β / pre-next-phase. Becomes relevant alongside M10 realistic-slippage when transitioning to deployment-realism. | **Audit finding 2026-05-29:** `_cost_sensitivity_sharpe` is in `metrics.py` and called per strategy. Computed values surface in `backtest_results.csv` columns but no apparent downstream consumer drives a strategy-deployment decision based on the 20bps Sharpe. If 20bps Sharpe < 0 means the strategy is execution-cost-fragile and shouldn't deploy live; that gate doesn't exist anywhere. **Action:** decide whether 20bps Sharpe should be a deployment gate (similar to 9-criteria) or just informational. If gate, add to DEC-426 5-Gate as a 6th gate. |
| 0 | `audit-meta-fix-scripts-layer` | **META-FINDING + placeholder sweep**: extend audit lens to scripts/* layer + resolve all `# placeholder` debt | PARTIAL-RESOLVED-OWNER-GATED-REMAINDER | **Status as of Batch 495 (2026-05-30):** Of 4 live-impact placeholders surfaced in the 2026-05-29 sweep: **(1) RESOLVED Batch 457 AU1** — `scripts/optimize_strategies_from_cube.py:130` PSR placeholder replaced by real `metrics._deflated_sharpe`. **(2) RESOLVED Batch 457 AU1** — `cube_populator.py:159` same fix landed in same commit. **(3) REMAINS owner-gated** — `backtest/engine/exit_context.py:335` `exit_regime` defaults to entry regime (PIT-correct exit-day regime requires separate lookup; intentional placeholder until owner approves the lookup). **(4) REMAINS** — `scripts/run_phase_1b_alpha_smoke.py:129` hardcodes `regime: "bull"` (structural smoke, not behavioral; tests that need real regime exercise engine path). **AU3 Batch 459** extended `scripts/build_verification_matrix.py` to scan `scripts/*` for the wired-means-engine-consumed audit. **AU6 Batch 462** shipped semantic-integration tests including "synthetic-strong-edge data passes 5-Gate" -- would have caught PSR placeholder. **Batch 495 (2026-05-30):** banned-pattern preflight GUARD landed in `test_batch495_pattern3_closures.py` (3 tests): exact count of 3 live placeholders pinned, every site must match `ALLOWED_PLACEHOLDER_SITES` allow-list, AU1 regression guard fails the test if PSR placeholder reappears. Two remaining placeholders are documented + intentional; owner-decision required to resolve either. |
| 1 | `cell-count-static-100-r3-finding` | Investigate why L2 cell count stayed at 100 in R3 | RESOLVED | **Batch 500 (2026-05-31) diagnosis on merged R3 cube data (29,360 trades, 102 strategies, 1,531 tickers):** 7,068 (strategy x regime x sector) verdict cells; verdict breakdown = 6,994 INSUFFICIENT_SAMPLE (98.9%) + 70 FAIL_GATE_2_BONFERRONI + 4 FAIL_GATE_3_DSR + **0 PASS**. n-trade filter cascade: 7,068 total -> 1,682 with n>=5 -> 74 with n>=30 -> **0 with n>=100**. The binding constraint is NOT `--no-portfolio-cap` capping trades and NOT the n>=5 filter -- it is **sample-size sparsity across the (strategy x regime x sector) grid**. 102 strategies x ~7 regimes x ~11 sectors = ~7,800 cells; with only ~287 trades/strategy average, the cells are too sparse. The "100 cell" figure from R2/R3 reports was the post-n>=5 subset of cells that also met confidence/sector eligibility -- coincidentally stable because the sparse-cell denominator is the structural bottleneck. **Fix paths:** (a) aggregate at (strategy x regime), not (x sector), to consolidate trade volume per cell; (b) raise per-strategy trade volume by widening universe / extending dates; (c) accept sector-granularity needs more cube data. |
| 2 | `skipped-bucket-static-47-r3-finding` | Investigate why SKIPPED_AT_ENGINE bucket barely moved (48 -> 47) in R3 | RESOLVED | **Batch 500 (2026-05-31) diagnosis on merged R3 cube data:** `output_batch395_final/skipped_trades.csv` = 1,380,771 skip events. Top 5 reasons: (1) `ticker_already_open_concurrent_block_bug61` 685,846 (49.7%), (2) `no_next_bar` 228,945 (16.6%), (3) `portfolio_gate_ticker_already_in_portfolio` 163,723 (11.9%), (4) `stopout_cooldown_active_5d_dec018` 71,158 (5.2%), (5) `max_loss_cap_breach_dec135_-10.xx` cumulative ~85,000 (~6%). Queue claim of "liquidity_floor is dominant" was wrong -- 0 mentions of liquidity_floor in top 20. **Real binding constraint:** concurrent-position handling + bar-boundary issues, NOT liquidity. `--no-portfolio-cap` could not collapse the SKIPPED bucket because the dominant reasons are not portfolio-cap-related; they are per-ticker concurrent-block (49.7%) + raw next-bar availability (16.6%). **Fix paths:** (a) investigate `ticker_already_open_concurrent_block_bug61` -- name suggests a known bug surface; (b) investigate `no_next_bar` to see if it is end-of-data boundary noise or a deeper data gap; (c) review DEC-135 max-loss-cap firing pattern (6% of skips). Quick win: (a) likely worth fixing for 685k recovered trade candidates. |
| 3 | `smc-silent-failure-rootcause-r3-finding` | Inspect SMC silent-failure logs from next cube run to root-cause PRODUCER_LAYER_ZERO_LIKELY | INVESTIGATED-AWAIT-RERUN-WITH-BATCH-458-LOGGING | **Batch 500 (2026-05-31) diagnosis on R3 engine logs:** 24 occurrences of `smc_ict.compute_smc_signals returned empty/falsy` in `output_batch395_batch_1/batch395-engine.log` (Batch 416 outer-most logging at the screener->smc_ict call site). Distinct silent-producer set in R3 logs = `{smc_ict.compute_smc_signals}` only -- no other Layer-Zero producer is firing empty. **R3 cube ran 2026-05-28; Batch 458 deeper logging shipped 2026-05-29**, so the 8-site internal logging (FVG / OB / BOS-CHoCH / liquidity / retracements / swing-highs-lows / dealing-range / library-import) was NOT present during R3. Existing logs cannot root-cause further. **Owner directive 2026-05-31: no rerun** -- so deferred until owner approves R4 cube. Once R4 runs with Batch 458 logging active, grep for `silent-failure.*smc_ict` will name the failing compute block + exception class. **Likely candidates** per prior AMI/numba investigation: (a) numba version mismatch on AMI (smartmoneyconcepts hard-deps numba>=0.58.1; AMI may have older); (b) smartmoneyconcepts editable-install missing on AMI; (c) Python 3.13/3.14 ABI mismatch for vendored library wheels. |
| 5 | `cube-cell-metrics-expansion` | Expand L2 cell schema with 5 tiers of additional metrics | RESOLVED-ALL-TIERS-WIRED | **Batch 530 (2026-05-31) cube_populator wire-in LANDED** -- closes the "one-liner follow-on" note. Added try/except-guarded `out.update(compute_tier_b_metrics(trades))` + `out.update(compute_tier_cde_metrics(trades))` to `cube_populator.compute_cell_metrics`. Real-data smoke: bollinger_tight + bull cell jumps from ~17 keys (Tier A only) to 46 keys (Tier A + B + C/D/E). 7 unit tests in `test_batch530_cube_populator_tier_b_cde_wireup.py` pin Tier A regression-survival, Tier B/CDE keys present, silent-failure when optional cols missing, empty-frame degrades, real-data smoke ≥30 keys, grep guard for both imports. **Batch 489** shipped Tier A subset (pnl_concentration + capacity flags) into `cube_populator.compute_cell_metrics`. **Batch 497** shipped Tier B (`cube_metrics_tier_b.py`): smart-money slice, days-to-earnings bands, regime/confidence/sector/aaii/circuit-breaker/macro-score buckets (16 tests). **Batch 504 (2026-05-31) ships Tier C + D + E** in new module `backtest/results/cube_metrics_tier_cde.py`. **Tier C (new computation):** `compute_sharpe_ci_95` (bootstrap 95% CI on Sharpe, seeded), `compute_oos_decay` (IS/OOS chronological split + decay diagnostic), `compute_effective_n` (autocorr-adjusted n_eff = n * (1-rho)/(1+rho)). **Tier D (composite scores):** `compute_sqn` (Van Tharp System Quality Number), `compute_k_ratio` (Linda Bradford Raschke equity-curve linearity), `compute_mar_ratio` (annualized return / abs(max DD)). **Tier E (risk management):** `compute_kelly_fraction` (optimal bet-size bounded [0,1]), `compute_cvar` (conditional VaR 5pct tail), `compute_risk_of_ruin` (probability of touching zero given current edge). Top-level `compute_tier_cde_metrics(trades)` aggregator. 30 new tests in `test_batch504_cube_metrics_tier_cde.py`. **Cube populator wire-in is a one-liner follow-on** -- `out.update(compute_tier_b_metrics(trades)); out.update(compute_tier_cde_metrics(trades))`. |
| 6 | `cube-walk-forward-r3` | Run `scripts/walk_forward_batch414_cells.py` on R3 cube cells | RESOLVED | **Batch 500 (2026-05-31):** EXECUTED on merged R3 cube. 9 Batch-414 candidate strategies x 4 folds (IS/OOS expanding-window per DEC-505). Output: `output_batch395_final/walk_forward_batch414_cells.json` (regenerated). Result: max OOS Sharpe across all 9 strategies x 4 folds = **0.406** (`bollinger_tight`). All 9 strategies FAIL the >=0.7 threshold in every fold. Per-strategy max OOS Sharpe: bollinger_tight 0.406, pead_long 0.325, cmf_flip 0.319, monthly_bias_momentum_long 0.314, xs_momentum_top_decile 0.283, pairs_mean_reversion_long 0.279, adx_initiation 0.244, xs_quality_top_quintile_long 0.230, xs_low_beta_long 0.227. Strategy-level walk-forward (102 strategies) verdict distribution: 53 INSUFFICIENT_OOS_DATA, 45 FAILS_BOTH, 4 WEAK, **0 PASS**. |
| 7 | `cube-analyst-overlay` | Emit single-run analyst overlays from merged cube trade log so all dashboard tabs have data | PARTIAL-RESOLVED-CORE-OVERLAYS-SHIPPED | **Batch 499 (2026-05-31):** `scripts/analyst_overlay_from_trade_log.py` SHIPPED. Reads any cube trade-log + reconstructs daily equity_curve via cumulative pnl_dollar groupby exit_date + emits 3 overlay files: `equity_curve.parquet`, `portfolio_metrics_overlay.json` (total_return / max_drawdown / ending_equity / n_trades_closed), `strategy_regime_matrix_overlay.json` (nested {strategy: {regime: {wr, n, avg_pnl_pct}}}). Skip-on-exists default + `--overwrite` flag. 14 new tests in `test_batch499_analyst_overlay.py`. **Unblocks dashboard Tab 2 Regime + Tab 4 Equity once owner runs the overlay pass.** Remaining overlays (walk-forward / bootstrap / congressional) surface as queue item #6 + Phase 1B work. **Owner-gated unblock:** `python scripts/analyst_overlay_from_trade_log.py --trade-log output_batch395_final/trade_log.csv --output-dir output_batch395_final/ --starting-capital 100000` then dashboard rebuild. |
| 8 | `post-run-validation-rc1` | Investigate post-run validation rc=1 from R3 merge | RESOLVED | **Batch 500 (2026-05-31) RESYNC EXECUTED:** owner-approved S3 resync at 2026-05-31. Confirmed S3 has full outputs (78 files per batch). Ran `aws s3 sync s3://stock-picks-batch395-jm-7421/outputs/batch_<N>/ output_batch395_batch_<N>/` for N=1..5 in parallel (all completed; 77-80 files per local batch dir). Ran `scripts/merge_batch_outputs.py` -> `output_batch395_final/` now populated with merged `trade_log.csv` (29,360 trades), `verdict_cube.csv` (7,068 cells across 102 strategies x regimes x sectors), 20+ analytical artifacts. Validation warning: 6 duplicate trades (cross-batch overlap, harmless). Ran analyst overlay -> `equity_curve.parquet` + `portfolio_metrics_overlay.json` + `strategy_regime_matrix_overlay.json` (36->102 strategy expansion). **UNBLOCKED:** items #1 (RESOLVED), #2 (RESOLVED), #3 (INVESTIGATED-AWAIT-RERUN-WITH-BATCH-458-LOGGING), #7 (overlays re-run on merged log), #6 walk-forward + #9 1A-α gate decision now actionable. |
| OPT-A | `producer-caching-sweep` | **OPT-A** apply B534 in-memory cache pattern to ALL per-ticker producers | RESOLVED-SHIPPED | **Batches 534-536 (2026-06-01/02) all SHIPPED + merged.** B534: pre-index patent + donors via identity-check; SEC EDGAR decoded cache. B535: per-ticker caches for housetrading / gov_contracts / lobbying / offexchange / short_interest / pytrends / financials (cross_sectional) / news (B546 extended). B536: smart_money _PREFETCH_CACHE + _BULK_INDEX. Cumulative effect: patentmomentum 240ms->7ms, smart_money_score 199ms->101ms (B536). All parity gates green; >100 unit tests across the sweep. |
| OPT-B | `cross-ticker-vectorization` | **OPT-B** panel-style cross-ticker signal computation | RESOLVED-SHIPPED | **Batches 537-538 (2026-06-02) SHIPPED + merged.** B537: `backtest/signals/technical_panel.py` with compute_rsi_panel + compute_simple_returns_panel + compute_ema_sma_panel (composite keys matching per-ticker schema exactly: `ema_<f>_<s>_bullish` / `_golden_cross` / `_death_cross` / `price_above_ema_<f>`). B538: wire-in via `USE_PANEL_TECHNICAL_SIGNALS` flag (default ON post-validation); screen_universe pre-builds close_panel + dispatches to compute_panel_signals_for_as_of; screen_instrument accepts panel_signals kwarg + passes skip_indicators={rsi,ema_sma,simple_returns} to compute_all_signals. Panel parity gate green. 8 unit tests + parity test. |
| OPT-C | `polars-panel-engine -> Numba JIT pivot` | **OPT-C** Numba JIT on hot indicator inner loops + cache pre-processing + SMC panel-cache | RESOLVED-PHASE-1-2-3-4-SHIPPED-FLAG-GATED | **Polars hypothesis empirically rejected (B543 finding: naive Polars 4.3x SLOWER than pandas at N=388).** Owner pivot directive 2026-06-02 "2" -> Numba JIT. **Phase 1 (B544):** compute_supertrend Numba JIT = 67x speedup. **Phase 2 (B545+B545b):** compute_parabolic_sar Numba JIT (200x), compute_hull_ma np.convolve WMA (30-150x), compute_break_retest TR vectorize (50-100x). **Phase 3 (B546-B552):** B546 news_sentiment date pre-convert (74ms->46ms); B547 OBV np.sign vectorize; B548 congressional pre-processed cache (31ms->6ms); B549 institutional pre-processed cache + outer-join vectorize for fund classification (29ms->5ms); B550 insider pre-processed cache (31ms->5ms); B551 pairs_trading parquet-read caches (65ms->11ms); B552 news article-score pre-compute + _score_window vectorize (46ms->26ms). **Phase 4 (B554-B560):** SMC primitive panel-cache module (B554), wire-in flag (B555 USE_SMC_PANEL_CACHE default False), B560 semantic A/B validation pass. **B560 VERDICT** (987 ticker x as_of pairs): wall-time 1.68x (428.9s -> 255.5s = 40pct reduction CONFIRMED). Key-set parity preserved. BUT per-pair signal divergence is 100pct (every pair differs by at least one signal). Per-key bool divergence: smc_equal_highs_swept 37.5pct, smc_ob_*active 17-22pct, smc_choch_* 11-12pct, smc_bos_bullish 16.7pct, smc_liquidity_swept_* 9-11pct, smc_ote_* 7-10pct. Float: smc_retracement_pct 99.8pct (20-bar iloc[-1] offset). Root cause: swing-detection lookahead + OB forward-mutation residual (documented in B555). **RECOMMENDATION: flag stays default False** -- owner can flip explicitly after running a full-cube comparison and accepting the semantic shift (would re-baseline Phase 1A-beta cube). **Cumulative profile: 1288s baseline -> ~480s post-B552 (60pct speedup); ~290s if flag flipped (78pct).** |
| OPT-C-FLAG | `smc-panel-cache-flag-flip-decision` | **Owner decision required**: flip `config.USE_SMC_PANEL_CACHE` from False to True? B560 validation (2026-06-02) confirmed 40pct wall reduction (428.9s -> 255.5s on 20-ticker x 33-bar harness) BUT 100pct of 987 (ticker x as_of) pairs differ on at least one signal value vs uncached path. Per-key bool divergence: smc_equal_highs_swept 37.5pct, smc_ob_*active 17-22pct, smc_choch_* 11-12pct, smc_bos_bullish 16.7pct, smc_liquidity_swept_* 9-11pct, smc_ote_* 7-10pct. Float: smc_retracement_pct 99.8pct. Root cause: swing-detection lookahead + OB forward-mutation residual (B555 docs). Flipping the flag re-baselines Phase 1A-beta cube semantics. | AWAITING-OWNER-SEMANTIC-ACCEPTANCE | **Recommendation: flag stays default False** until owner runs a small-cube cache-ON vs cache-OFF comparison and accepts the semantic shift. Speed gain doesn't override correctness concern at this stage. Owner options: (a) keep False permanently -- 1.68x SMC speedup forgone but semantic parity preserved (cumulative project: ~480s post-B552 baseline); (b) flip True after dashboard verdict comparison -- cumulative ~290s = 78pct speedup; (c) library-level refactor of smartmoneyconcepts swing_highs_lows to remove forward-mutation -- multi-day work. Pin: `test_batch560_opt_c_smc_cache_semantic_validation.py::test_batch560_flag_default_remains_false`. |
| OPT-D | `precomputed-signals-pipeline` | **OPT-D** Pre-computed signals materialization | RESOLVED-SHIPPED | **Batches 539-542 (2026-06-02) SHIPPED + merged.** B539: `backtest/signals/precomputed_cache.py` + `scripts/precompute_signals.py` (CLI: `--tickers AAPL,MSFT|ALL` `--start --end` `--ohlcv-cache-dir`). B541: screen_instrument cache-lookup-first path. B542: flag flips `USE_PRECOMPUTED_SIGNALS=True` after validation. Cache MISS falls back to compute path. |
| 9 | `1a-alpha-gate-decision-r3` | Owner decision on 1A-α gate state after R3 walk-forward | R4-READY-AWAITING-OWNER-LAUNCH-APPROVAL | **2026-06-02 status update**: OPT series COMPLETE (OPT-A B534-536, OPT-B B537-538, OPT-C B544-552+B554-560, OPT-D B539-542). Producer fixes shipped (B556 SMC sparse-event UNBLOCKS 18 strategies; B559 morning_star precedence; B561+B561a sector_history 14/14 cohort UNBLOCKS 10 classification strategies). Cumulative profile speedup confirmed: 1288s -> ~480s = 60pct. R4 wall projection on c7a.16xlarge: 30-60min ~ $3-5 (well under $25 budget). **R4 launch unblocked -- needs explicit owner approval per CLAUDE.md paid-AWS rule.** When launched: cascades into auto-resolution of items #3 (SMC silent-failure logs), #7 (overlay re-run), #11 (deprecation OOS data). | **Owner directive 2026-06-01:** R4 BLOCKED until OPT-A producer caching sweep ships. Smoke 1+2 empirical data revealed R4 architecture is ~7x slower per-bar than R3 baseline due to per-ticker disk reads in 8-10 producers. Without OPT-A, any 128-vCPU R4 config costs ~$220 wall. After OPT-A, projected ~$10-15. **Prior 2026-05-31:** HOLD R4 cube run until P17 sleeves wire-in lands. Reasoning: P17a scoped extraction is in flight (~22%, ETA 6-8h); when complete + B526 validator green + sleeve registration batch ships, R4 cube includes SEC EDGAR signal contribution alongside R4 config + Batch 521 multi-feature gates. Auto-trigger: P17 wire-in batch ships on P17a completion; R4 cube unpause approval requested after that. **Batch 521 (2026-05-31) multi-feature optimizer output** -- pairwise (feature_A_bucket × feature_B_bucket) extension of Batch 501. 450 valid pair rows. **Top incremental_lift findings (joint Sharpe beats BOTH single-feature Sharpes):** (1) `pairs_mean_reversion_long + sm_score_le_0 + regime_bear` baseline 0.56 -> single best 2.12 -> joint **5.17** (n=61, +3.05 incremental); (2) `adx_initiation + macro_neutral + tier_LOW` baseline -0.40 -> single best 1.84 -> joint **3.70** (n=42, +1.86); (3) `pairs_mean_reversion_long + macro_neutral + sentiment_positive` baseline 0.56 -> single best 1.67 -> joint **3.47** (n=67, +1.80); (4) `monthly_bias_momentum_long + macro_positive + regime_neutral` baseline 0.80 -> single best 3.90 -> joint **5.60** (n=62, +1.70). Output: `output_batch395_final/entry_threshold_multifeature_candidates.csv`. 9 unit tests in `test_batch521_multi_feature_entry_optimizer.py`. Owner-pick layer over R4 cube for multi-feature entry gates. **Batch 514 (2026-05-31) R4 CONFIG ACTIVATED** per owner directive: (1) `BUG_61_BLOCK_MODE` flipped from "ticker" -> **"ticker_strategy"** (recovers 685k blocked candidates; different strategies stack on same ticker); (2) `STRATEGY_REQUIRED_MACRO_REGIME` populated with 5 owner-approved entries (`bollinger_tight`, `monthly_bias_momentum_long`, `xs_quality_top_quintile_long`, `pead_long`, `adx_initiation` -> "neutral"). Test pins updated to assert active values (any change forces owner sign-off). **Cube R4 run PAUSED by owner till further notice.** When unpaused: cube spawns with the activated config -> macro_neutral entry filter on 5 candidates + cross-strategy ticker stacking allowed. Expected R4 trade volume materially higher than R3 (685k recovery + multi-strategy fan-out) -> escapes sample-size sparsity per item #1 -> larger n per cell -> Bonferroni / PSR have more power. | **Batch 510a (2026-05-31) BUG-61 fix SHIPPED** per owner directive: `config.BUG_61_BLOCK_MODE` flag added (default "ticker" preserves prior Option A behavior bit-identically; modes "ticker_direction" / "ticker_strategy" / "off" available). Engine `backtest.py:1230+` 4-branch dispatch. 11 new tests in `test_batch510a_bug61_block_modes.py`. **Recovers up to 685k blocked candidates per item #2** when owner flips the mode for R4. **Batch 501 (2026-05-31) entry-side optimizer output:** **MAJOR FINDING -- `macro_score == neutral` is the dominant entry-side lift across 5 of 9 candidate strategies.** Top filters: `bollinger_tight + macro_neutral` Sharpe -0.06 -> +4.41 (n=36); `monthly_bias_momentum_long + regime_neutral` 0.80 -> 3.90 (n=82); `xs_quality_top_quintile_long + macro_neutral` 0.21 -> 3.07 (n=76); `pead_long + macro_neutral` 1.13 -> 3.40 (n=58); `xs_low_beta_long + sentiment_negative` 0.34 -> 2.74 (n=165); `adx_initiation + macro_neutral` -0.40 -> 1.84 (n=138). **Batch 500 (2026-05-31):** R3 max OOS Sharpe 0.406 < 0.7; 1A-α LOCKED. **Owner directive 2026-05-31:** R4 blocked till explicit approval. R4 spec needs owner sign-off on (1) flipping `BUG_61_BLOCK_MODE` to "ticker_strategy" or "ticker_direction"; (2) macro_neutral entry filter wiring (Batch 510b spec). |
| 10 | `phase-1b-alpha-launch` | Phase 1B-α agent overlay run ($300 Haiku budget) | DEFERRED | Owner-gated behind 1A-α gate (item #9). Scope: agents-only validation per owner directive 2026-05-26 — strategies and exits decided in Phase 1A-β. |
| 11 | `25-negative-sharpe-deprecation` | All-negative-Sharpe strategies -- deprecation decision | DEFERRED-PENDING-R4-OOS-DATA | **Owner decision 2026-05-31 (Batch 505):** defer all deprecation decisions until R4 OOS data lands. R4 will rerun with `macro_score == neutral` entry filter + ticker_already_open bug fix per #9 unlock; the 31 currently-FAILS_BOTH candidates may shift verdicts under the new filter. Re-surface for decision after R4 walk-forward. **Batch 500 (2026-05-31) per item #6 walk-forward data:** R3 cube has 63 strategies with negative IS sharpe_ratio (vs prior 25 estimate; reflects 102-strategy universe). 31 meet empirical-deprecation criteria (n>=100 + FAILS_BOTH IS+OOS): worst offenders po3_bearish (-1.34, n=1766), xs_momentum_bottom_decile_short (-0.73, n=2163), avwap_50_reclaim (-0.63, n=1276), ichimoku_cloud_breakout (-0.49, n=384), pead_short (-0.47, n=453). 5 INSUFFICIENT_OOS_DATA. Full evidence retained in walk_forward_validation.csv. |
| 12 | `unarchive-list-eval` | Owner unarchive-list evaluation | RESOLVED-NONE-UNARCHIVED | **Owner decision 2026-05-31 (Batch 505):** none of the 23 archived docs to be unarchived. All retain-as-archive. Item closed. **Batch 500 inventory:** 23 docs at `archive/2026-05-28-pre-1a-alpha-gate/docs/`; 8 forward-looking candidates were surfaced (AUDIT_TRIAGE / IMPLEMENTATION_READINESS_DASHBOARD / PASS_53_PRIORITIES / PHASE_1A_BETA_QUIET_STRATEGY_FORENSIC / PHASE_1A_BETA_STAGE_D_LOSER_CELL_AUDIT / POST_MAY_29_OPERATION_GUIDE / PREFETCH_COVERAGE_AUDIT / THEME_X53_SEQUENCING) -- owner reviewed + declined all. |
| 13 | `cube-merge-rebuild-r3` | R3 cube re-run COMPLETE + merge + optimizer + rebuild | RESOLVED | All 5 batches `_COMPLETE` 2026-05-29; merge bg `bd01668y9` succeeded; optimizer ran into `output_optimization_candidates_2026_05_29/`; dashboard rebuilt (commit `a7d2c0a4d`); Tab 16 Cube Diff populated; Tab 17/18 added (commit `3eb546b6e`). R3 trades = 29,360 (+201 vs R2). |
| 14 | `dashboard-batch446-verify` | Verify dashboard post-Batches 430/433/434/437/438/440/441/442/443/445/446 | RESOLVED | All shipped through Batch 446 (this turn). Includes PSR-hardcoded-False audit finding + metrics.py consolidation queue item. Hard-refresh to verify. Will REOPEN if owner reports specific tab issue. |
| AU1 | `5-pattern-audit-cube-populator-psr-placeholder` | **Pattern 1 #2 hit**: `backtest/results/cube_populator.py:159` PSR placeholder | RESOLVED | **Batch 457 (2026-05-29):** wired `metrics.py::_deflated_sharpe` (Bailey & Lopez de Prado 2014) into `cube_populator.compute_cell_metrics`; added `skew` + `kurtosis` to per-cell stats; PSR field now real-computed not placeholder. Semantic-integration tests `backtest/tests/test_batch457_psr_wireup.py` (8 tests pass) assert: (a) PSR equals `_deflated_sharpe` output, (b) PSR varies across edge strengths, (c) strict 5-Gate CAN pass on synthetic strong-edge data, (d) cube_populator + optimize_script produce identical PSR for identical inputs. Fixed in same commit as queue item #4. **Tests:** ✅ (8 new + 13 existing cube_populator). **Wired:** ✅ (engine call path: `populate_cube` → `compute_cell_metrics` → `_deflated_sharpe`). **Activated:** ✅ (next cube build emits real PSR). |
| 4 | `cube-cell-psr-hardcoded-false-P0` | **P0 BUG**: PSR gate in `optimize_strategies_from_cube.py::_dec426_verdict` hardcoded False | RESOLVED | **Batch 457 (2026-05-29):** wired `metrics.py::_deflated_sharpe` (Bailey & Lopez de Prado 2014) into `_cell_stats` + `_dec426_verdict`; added `skew` + `kurtosis` per cell; PSR gate now real-computed not False. Semantic-integration tests `backtest/tests/test_batch457_psr_wireup.py` (8 tests pass) assert strict 5-Gate CAN pass on synthetic strong-edge data, PSR is real-computed not placeholder, and the two consumer modules (optimize_strategies_from_cube + cube_populator) produce identical PSR for identical inputs (single source of truth: metrics.py). Re-run optimizer on R3 cube next turn to confirm strict 5-Gate pass count rises from 0 to some N. **Tests:** ✅ (8 new). **Wired:** ✅ (engine call path: `_cell_stats` → `_dec426_verdict` → `_deflated_sharpe`). **Activated:** ✅ (next optimizer run emits real PSR; expected 1A-α gate decision re-eval). |
| AU2 | `5-pattern-audit-silent-exception-sweep-critical-paths` | **Pattern 1 #3 hit**: silent except sweep across critical engine + signals + results path | RESOLVED | **Batch 458 (2026-05-29):** verified actual count = 51 across 7 critical files (queue claim "132+" was overcount; rebuilt by parser counting only true `pass/continue/return-default` after `except`). Classified each: **22 of 51 = category (b)** "needs rate-limited diagnostic logging" — converted to one-shot logger calls via new shared helper `backtest/util/silent_failure_logger.py` (generalises Batch 416 screener-private pattern). Breakdown: smc_ict.py 8 of 8 sites (closes queue item #3 PRODUCER_LAYER_ZERO_LIKELY); screener.py 13 additional producer wrappers (pead / insider_buying / institutional_persistence / macro_events / chart_patterns / index_rebalance / pairs_trading / news_sentiment / calendar_effects / cross_asset / volume_profile / multi_timeframe / cross_sectional_features); exit_strategies.py 1 vectorized-import fallback. **29 of 51 = category (a)** defensive-justified and stay silent: TypeError/ValueError date-coercion in backtest.py/metrics.py/exit_context.py, ImportError alt-library cascades in cpcv.py, pool-worker pre-warm fallbacks in screener.py. **Tests:** ✅ 9 new in `backtest/tests/test_batch458_silent_failure_logger.py` (unit dedup + reset + cross-component + semantic-integration that monkey-patches the SMC library to raise and asserts the wired call path fires the logger, not just a greppable-string check). **Wired:** ✅ engine + signal call paths import + invoke helper (smc_ict module-level import + 7 compute blocks; screener.py 13 producer wrappers; exit_strategies vectorized-import). **Activated:** ✅ next cube/AWS run logs will surface previously-invisible producer failures, one line per (component, exception-type) per process. |
| AU3 | `5-pattern-audit-verification-matrix-scripts-layer-extension` | **Pattern 4 confirmed**: VERIFICATION_MATRIX never covered `scripts/*` (optimizer / walk-forward / merge / dashboard build) | RESOLVED-REGEN-LANDED | **Batch 523 (2026-05-31) regen LANDED:** captured optimizer canonical run via `python -m coverage run --data-file=.coverage_optimizer scripts/optimize_strategies_from_cube.py --input-dir output_batch395_final` -> `coverage_report_optimizer.json` (2.8MB, gitignored). Re-ran `python scripts/build_verification_matrix.py` -> merged base + optimizer coverage -> regenerated VERIFICATION_MATRIX.md + verification_matrix.json. **Post-regen item count: 736**; **engine distribution:** YES=165, FUNC-DEAD=25, NO=27, PARTIAL-ORPHAN=105, DECLARED-ONLY=17, LAZY-WIRED=6, N/A=391. **BUG-018 remains FUNC-DEAD** under the optimizer canonical run -- the Bonferroni branch isn't reached in the 102-strategy small-cube run; AU10 KNOWN_GAP narrative still holds. 6-test drift-pin in `test_batch523_verification_matrix_regen_drift.py` (item-count floor 700, schema, BUG-018 status pin, lattice completeness, .gitignore guard for `coverage_report_*.json` + `.coverage_*`). **Batch 459 (2026-05-29):** extended `scripts/build_verification_matrix.py` so `scripts/*.py` lives in the matrix's source-file scan, `is_engine_consumed` accepts both `backtest/` and `scripts/` tag paths, and `load_coverage()` MERGES all `coverage_report*.json` files in the repo root (union executed_lines / intersect missing_lines / recompute percent). Import-graph walks (`_files_imported_by`, `_symbol_consumed_externally`) now traverse `scripts/` too so a `backtest/*` helper imported only by the optimizer registers as wired. Matrix MD header documents the two canonical runs (`backtest/run_phase1a.py --tickers AAPL ...` + `scripts/optimize_strategies_from_cube.py ...`) and tells the operator to drop additional `coverage_report_<tag>.json` files alongside to extend coverage. **Tests:** ✅ 7 new in `backtest/tests/test_batch459_verification_matrix_scripts_layer.py` (collect_source_files includes scripts; is_engine_consumed accepts scripts/* tag; load_coverage merges two synthetic reports with union/intersect/percent semantics; single-report back-compat; empty-state safety; MD documents dual canonical runs; _files_imported_by walks scripts/). **Wired:** ✅ matrix builder reads the merged coverage during normal `python scripts/build_verification_matrix.py` invocation. **Activated:** ✅ on next matrix regen with the optimizer canonical run captured, scripts/* DEC/BUG tags move from `engine=N/A` to a real YES/NO/LAZY-WIRED/FUNC-DEAD verdict; closes Pattern 4 for the cube-optimizer call path. Also unblocks queue item AU10 (BUG-RESOLVED engine: UNKNOWN gap). |
| AU4 | `5-pattern-audit-equity-curve-parallel-universe-investigation` | **Pattern 5 candidate** -- investigated and found NOT a parallel universe | RESOLVED | **Batch 460 (2026-05-29):** investigated and found the queue framing was wrong. The name `equity_curve` appears in 8 files but maps to exactly TWO semantically-distinct entities + ONE generic helper: (1) **Portfolio.equity_curve** at `backtest/engine/portfolio.py:~140` -- canonical `list[(date, equity_dollar)]` appended by mark_to_market, consumed by writer.py / metrics.compute_portfolio_metrics_from_curves / agents.our_risk_toolkit; (2) **per-strategy compounded curve** at `backtest/results/metrics.py:~2222` -- `(1+pnl/100).cumprod()` pd.Series of multipliers, consumed only by _adf_test (DEC-414) + _chow_test (DEC-416) for per-strategy stationarity; (3) generic `max_drawdown(equity_curve: Sequence[float])` at `quant_audit.py:69` -- scale-invariant utility that operates on either. backtest.py mentions the name only in comments referencing the Portfolio path -- NOT a third definition. **No consolidation needed:** the two real entities are intentionally distinct (different unit, different scope, different math). Merging would break either stationarity testing OR portfolio Sharpe. **Action taken:** added explicit docstrings at portfolio.py:~140 and a comment at metrics.py:~2222 documenting the distinction so future readers don't conflate. **Tests:** ✅ 7 new in `backtest/tests/test_batch460_equity_curve_distinct_definitions.py` (portfolio curve shape + per-strategy curve shape + non-interchangeability + scale-invariant helper + docstring wiring guards + backtest.py has no rogue local definition). **Wired:** ✅ no engine change needed -- consumers were already correctly routed. **Activated:** ✅ documentation guards prevent future regression; same lesson applies to AU5 (next item). |
| AU5 | `5-pattern-audit-portfolio-metrics-parallel-universe-investigation` | **Pattern 5 candidate** -- investigated and found NOT a parallel universe | RESOLVED | **Batch 461 (2026-05-29):** investigated and found the queue claim of "6 files" was wrong by inspection. Real layout across non-test code: (1) `backtest/results/metrics.py:2718` -- SINGLE computation `compute_portfolio_metrics_from_curves(...)`; (2) `backtest/results/writer.py:~1059` -- SINGLE writer (calls the computer then json.dumps to `portfolio_metrics.json`); (3) `scripts/build_dashboard_phase_1a.py:~361` -- SINGLE reader (`load_json("portfolio_metrics.json")`, does NOT recompute); (4) `backtest/engine/backtest.py:2364` -- comment only; (5) `backtest/engine/portfolio.py:~143` -- docstring only (added in Batch 460). The 3 scripts the queue named (merge_batch_outputs.py / run_t0_close_out.py / verify_batch_69_phase_3.py) contain **ZERO** references to portfolio_metrics. **No consolidation needed:** the single-source flow (metrics -> writer -> dashboard) is already correct. **Action taken:** added docstring at `compute_portfolio_metrics_from_curves` explicitly marking it as the SINGLE SOURCE OF TRUTH and naming the canonical consumer chain. **Tests:** ✅ 7 new in `backtest/tests/test_batch461_portfolio_metrics_single_source.py` (only one def of the computer; only one writer of the JSON; dashboard is reader-not-recomputer; queue-named scripts contain zero references; metrics docstring marks single source of truth; writer.py imports + calls the computer; defensive scan finds zero suspicious recomputation patterns). **Wired:** ✅ no engine change needed. **Activated:** ✅ greppable docstring + 7 tests guard the single-source chain against future drift. |
| AU6 | `5-pattern-audit-semantic-integration-test-framework` | **Pattern 3 mitigation**: semantic-integration test framework | RESOLVED | **Batch 462 (2026-05-29):** created `backtest/tests/test_semantic_integration_pyramid.py` with 8 tests covering the 5 audit-gap findings from the queue spec: **M3** adversarial random-walk -> 40 trials produce ZERO strict 5-Gate passes (closes "missing-random-walk-adversarial-baseline"); **positive control** synthetic strong-edge data DOES pass 5-Gate (two-sided check); **0b** Bonferroni denominator runtime > 1000 via production-callsite assertion `M = max(len(fired) * 9, 1)` + `m_total_candidates=M` (closes "pattern-3-bonferroni-denominator"); **0c** walk-forward `FOLDS` chronological + disjoint OOS invariants (closes "pattern-3-walk-forward-chronological-split"; both per-fold IS_end == OOS_start and cross-fold OOS disjointness asserted, plus DEC-505 1y span check); **#4+AU1** PSR varies with edge strength (closes "cube-cell-psr-hardcoded-false"); **framework discoverability** test asserts the GAPS CLOSED docstring block stays present so future audit findings get appended to this file rather than living as one-offs. **Tests:** ✅ 8 new, all pass on 2876-test pyramid baseline. **Wired:** ✅ runs in the standard `pytest backtest/tests/` pyramid; semantic-integration tier under CHECKLIST #100 #69. **Activated:** ✅ next audit finding whose check is "verdict is meaningful" appends here, preventing the "tests pin file existence but not invariants" pattern. |
| AU7 | `dead-code-sweep-config-flags-unused` | **Round 2 dead-code sweep**: unused config flags surfaced + classified, no autonomous deletion | RESOLVED | **Batch 463 (2026-05-29):** verified actual unused-constant count is **102, not 18** (queue claim was a subset). Created `backtest/tests/test_batch463_unused_config_flags_registry.py` with a full classification registry mapping each of the 102 to one of: **(a) implement now** (~8: WALK_FORWARD_FOLDS, TIER_3_POSITION_SIZE_PCT, CASH_MANAGEMENT_TRIGGER_PCT, DROPPED_STRATEGY_REEVAL_DAYS, TIER_3_MAX_TICKERS, RR_RATIO_MINIMUM, BURST_DAY_STRESS_START_YEAR, SECTOR_PASSING_CRITERIA -- each is a Phase 1A-β build candidate); **(b) delete** (~21 truly orphaned -- BATCH_64_* / DEC_*_SUPERSEDED_BY / FORK_FIRST_PRINCIPLE_NOTE / etc.); **(c) defer** (~25 -- Phase 1B+/Stage 4 features like EMAIL_*, AB_*, ORTEX_*); **(d) documentation** (~20 -- string/dict constants documenting a decision rather than parameterizing code); **(e) schema-spec** (~23 -- cache schemas + API tier docs referenced externally but not by runtime code). **No autonomous deletions** per CLAUDE.md owner-approval rule -- the registry is the surface for owner to approve individual (a/b/c) actions in follow-up batches. **Tests:** ✅ 4 new (registry covers all unused / no stale entries / legal class letters / count matches actual sweep). **Wired:** ✅ test scans live config.py every run; drift surfaces immediately. **Activated:** ✅ any new unreferenced constant added to config.py without a registry entry fails the test; forces classification before merge. |
| AU8 | `dead-code-sweep-writer-outputs-write-only` | **Round 2 dead-code sweep**: write-only writer.py outputs surfaced + classified, no autonomous deletion | RESOLVED | **Batch 464 (2026-05-29):** verified actual write-only output count is **30, not 18** (queue claim was a subset). Strict scan (read_csv / read_parquet / json.load / load_json / load_csv / load_parquet helpers / open(...'r')) across backtest/* + scripts/* non-test code shows 30 of 51 writer.py output filenames have ZERO python read site. Created `backtest/tests/test_batch464_writer_outputs_registry.py` with a classification registry mapping each of the 30 to one of: **(a) active artifact missing python reader** (~19: agent_performance.csv, top_losers_per_strategy.json, trade_pnl_decomposition.csv, verdict_cube.csv, trade_exit_detail.csv, exit_*.csv family, signal_fire_rates.json, sizing_log.csv, edge_decay_metrics.csv, etc. -- real outputs whose consumer is the dashboard data.js catalog or downstream tool outside scanned tree); **(b) DEC-stub placeholder** (~10: analyst_data_stub.json, batch163_stub_results.json, cache_freshness_checksum_stub.json, chart_pattern_skeleton_stub.json, fx_exposure_stub.json, sector_neutral_hedge_stub.json, short_long_conversion_stub.json, dec_constants_verification.json, yfinance_hardcut_verify.json, test_coverage_gate.json -- skeleton emitted for a DEC's eventual artifact); **(c) engine-internal forensic** (~2: benchmark_curve.parquet, stop_cluster_pattern.json -- debug evidence in run output dir). **No autonomous deletions** per CLAUDE.md owner-approval rule -- registry surfaces inventory for owner-approved follow-up. **Tests:** ✅ 4 new (registry covers all write-only / no stale entries / legal class letters / count matches scan). **Wired:** ✅ test scans live writer.py every run. **Activated:** ✅ any new write-only artifact added to writer.py without classification fails the test. |
| AU9 | `dead-code-sweep-orphan-scripts` | **Round 2 dead-code sweep**: orphan scripts surfaced + classified, no autonomous archival | RESOLVED | **Batch 465 (2026-05-29):** queue claim of 21 was very stale. Actual count = **5** on tracked scripts/*.py (excluding output_*/, archive/, logs/, tests/, plus EXECUTION_QUEUE.md + this registry test which self-reference orphans and create a circular "documented therefore not orphan" loop). The other 16 named in the queue were archived, wired, or renamed across intervening batches. Current orphans: (1) `scripts/aws_batch395_upload_data.py` -- one-time S3 upload pre-batch395; (2) `scripts/fix_meta_ticker_corruption.py` -- Batch 275 META data fix; (3) `scripts/phase_1b_canary_dashboard.py` -- Phase 1B canary dashboard consumer of canary_signals.parquet, needs launcher/cron before Phase 1B-alpha gate; (4) `scripts/audit_ohlcv_ticker_reassignments.py` -- Batch 275 META-style ticker-reassignment audit companion; (5) `scripts/run_phase_1a_beta_local.py` -- local Phase 1A-β runner shim, pre-AWS canonical path. Created `backtest/tests/test_batch465_orphan_scripts_registry.py` classifying each as **(a) intentional one-shot archive candidate** (aws_batch395_upload_data), **(b) ad-hoc data-fix retained for documented procedure** (fix_meta_ticker_corruption + audit_ohlcv_ticker_reassignments), **(c) active deliverable needs caller wiring or doc reference** (phase_1b_canary_dashboard + run_phase_1a_beta_local). **No autonomous archives** per CLAUDE.md owner-approval rule. Scan tightened post-pyramid-failure: TRACKED scripts only + excludes generated trees + self-reference files (queue.md + this test file) to keep the scan stable run-to-run. **Tests:** ✅ 4 new (registry covers + no stale + legal letters + count match). **Wired:** ✅ scan runs each test invocation. **Activated:** ✅ any new orphan added to scripts/* without classification fails the test. |
| C-DATA | `sector-history-csv-expansion` | **Phase 1A-β producer-zero forensic owner-gated DATA action**: expand `Backtesting universe/sector_history.csv` with GICS reclassifications 2021-2026. Source: S&P DJI press releases (primary), MSCI GICS revisions, S&P announcement archive. L88 manual-verification discipline. | RESOLVED-FULL-B561+B561a | **Batch 561 + 561a (2026-06-02) SHIPPED + merged** per owner directives 2026-06-02 "Execute" + "Execute follow on". B561 expanded sector_history.csv with 13 of 14 names from the 2023-03-17 GICS reclassification (cross-verified 5+ sources: S&P DJI Indexology Blog 2023-02-03, LSEG Lipper Alpha, ETF Strategy / Yahoo / Zacks / Nasdaq). B561a follow-on fetched FLT.parquet from Polygon via one-time `scripts/fetch_flt_one_time_b561.py` (706 rows 2021-06-03 to 2024-03-22, the pre-CPAY-rename window) and restored the FLT row pair. **ALL 14 of 14 affected S&P 500 names now in CSV.** Three cohorts: IT->Financials (V, MA, PYPL, FISV, FIS, GPN, JKHY, FLT -- 8 of 8), IT->Industrials (ADP, PAYX, BR -- 3 of 3), Consumer Discretionary->Consumer Staples (TGT, DG, DLTR -- 3 of 3). 14 pin tests green (8 in B561 + 6 in B557 updated). Empirical impact: producer emits `classification_change_from_tech=True` for 11 IT-cohort tickers + `to_defensive=True` for 3 retail-cohort tickers across 90-day windows post-2023-03-17 -- unblocking ~10 classification_change_* strategies in R4 cube. No 2024-2026 individual-name reclassifications found in cross-verification (cluster closed). |
| AU10 | `dead-code-sweep-bug-resolved-engine-unknown` | **Round 2 cross-audit**: BUG engine-status registry + UNKNOWN-gap claim resolved | RESOLVED | **Batch 466 (2026-05-29):** queue claim was stale + conflated dashboard display fallback with matrix engine status. Inspecting current verification_matrix.json: of the 7 named BUGs (014/015/016/018/022/023/133), **NONE** have engine: UNKNOWN -- the matrix doesn't even have an UNKNOWN bucket for engine (legal: YES/LAZY-WIRED/PARTIAL-ORPHAN/FUNC-DEAD/NO/DECLARED-ONLY/N/A). Current status: **BUG-014=N/A** (methodology), **BUG-015=YES**, **BUG-016=N/A** (methodology), **BUG-018=FUNC-DEAD** (real gap; Bonferroni code in optimizer not in AAPL backtest -- KNOWN_GAP, resolves once Batch 459 dual-source coverage regen captures coverage_report_optimizer.json), **BUG-022=YES**, **BUG-023=YES**, **BUG-133=YES**. Only BUG-018 is a real engine-consumption gap, and Batch 462 verified the production callsite passes M = max(len(fired) * 9, 1) so the code is correctly wired -- the FUNC-DEAD is a coverage-source-gap not a wiring-gap. Created `backtest/tests/test_batch466_bug_engine_status_registry.py` with 5 drift-guard tests: status registry maps each named BUG to its current engine status (regen drift surfaces immediately); KNOWN_GAPS allow-list documents the BUG-018 expected FUNC-DEAD with cross-reference to Batch 462; matrix-builder regression guard (no illegal engine values); reason-length hygiene; STATUS_INTERPRETATION must cover every engine value found in matrix. **No matrix regeneration in this batch** -- regen is owner-gated (canonical backtest + Batch 459 optimizer coverage capture). Registry surfaces the inventory; owner triggers regen. **Tests:** ✅ 5 new. **Wired:** ✅ tests read live verification_matrix.json. **Activated:** ✅ next regen that flips a status surfaces in CI rather than landing silently. |

---

## Completed log (DONE-ARCHIVED — truly closed; most recent first; one line per item)

- 2026-05-29 — `execution-queue-create` — Created EXECUTION_QUEUE.md + CHECKLIST #94 (Batch 432). Owner directive: bring order to chaos.
- 2026-05-29 — `archive-prefetch-api` — Batch 431 (a8b53cfb0): archived PREFETCH_COVERAGE_AUDIT.md + API_AUDIT.md after activity-check verified no Python read/write code refs.
- 2026-05-29 — `dashboard-batch430-fix` — Batch 430 (1af03f282): fixed buildTable `$(window).ready` SyntaxError that killed all DataTable renders + added marked.js for Tab 10 + structured render for Tab 11.
- 2026-05-29 — `archive-impl-readiness-dashboard` — Batch 429 (577177432): archived IMPLEMENTATION_READINESS_DASHBOARD.md (self-superseded).
- 2026-05-28 — `archive-batch318-quant` — Batch 428 (f4191bb7f): archived BATCH_318_PROCESS_POOL_DESIGN + QUANT_CORRECTNESS_AUDIT_DEC_246.
- 2026-05-28 — `archive-sprint1-readme` — Batch 427 (91b7cd4cd): archived SPRINT1_POLYGON_PREFETCH_README (Sprint 1 → 0A rename).
- 2026-05-28 — `archive-audit-triage-post-may29` — Batch 426 (27c512aee): archived AUDIT_TRIAGE.md + POST_MAY_29_OPERATION_GUIDE.md.
- 2026-05-28 — `archive-dated-audits-gitignore` — Batch 425 (574205bce): archived 8 dated audit docs + .gitignore additions (`backtest/agents/cache/`, `/tmp_*`, `/vm_*`) + 4 root tmp orphans deleted.
- 2026-05-28 — `cleanup-prior-outputs` — Batch (ec9c7082b): removed 735 tracked files from 17 superseded `output_*` dirs (2.4M deletion lines).
- 2026-05-28 — `monitoring-framework-update` — Batch (cc9246445): doc-sweep monitoring framework L4 stdout buffering + AWS PATH prereq + pre-launch protocol.
- 2026-05-28 — `cube-shape-a-launch` — Batches 424 (948769bf2) + parallel orchestrator: launched Shape A 3 × c7a.8xlarge spot cube re-run; all 5 batches completed 2026-05-29.

---

## Cross-references

- `CHECKLIST.md` #94 — mandatory per-turn queue update (rule 2 above).
- `MONITORING_FRAMEWORK.md` — L0-L7 stack (cube run monitor sits in L4 layer).
- `PHASE_1A_BETA_STATUS.md` — Phase 1A-β living doc (Stage 1-6 batch ship list).
- `PHASE_1A_BETA_CUBE_OPTIMIZATION_WORKFLOW.md` — locked 6-stage workflow.
- `archive/cube_rounds/rounds.json` — round-by-round registry (Batch 443) consumed by Tabs 17 + 18.
- `CLAUDE.md` HARD RULES — owner approval, no-new-md-without-approval (`feedback_no_write_only_md_files`), full-pyramid mandate (CHECKLIST #69/#93).
