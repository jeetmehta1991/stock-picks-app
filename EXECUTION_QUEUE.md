# EXECUTION_QUEUE.md — Project-level sequential execution queue

**Owner directive 2026-05-29 (Batch 432):** *"We have been jumping all over the place and the items get missed. Lets bring order to the chaos."*

**Owner directive 2026-05-29 (Batch 444, lifecycle clarification):** *"Resolved items need to go down in the queue after being tagged as resolved. If needs a new iteration, needs be tagged as reopened and to be moved to top of queue. Add findings to the queue document as well. Note that such items need to be added in the queue document at each turn! This is mandatory!!!"*

**Purpose.** Single ordered list of the next project-level execution items. The **top of the queue is the next thing Claude executes**. Items can be reordered, but whatever sits at the top still executes next. This is the project-level analogue of per-session todos — it survives across sessions.

**Distinction from todos.** TodoWrite tracks in-session task state for *one* conversation. This queue tracks *project-level* obligations across sessions. A todo is "what am I doing in this conversation"; a queue item is "what is the project's next milestone."

---

## Rules

1. **Top item is next execution.** When work is needed and the queue is non-empty, the top `PENDING` (or `REOPENED`) item runs next.
2. **Mandatory update each turn (CHECKLIST #94).** Every turn that produces meaningful changes — INCLUDING newly-discovered findings, bugs, follow-ups — must end with a queue sweep. Failure to update = non-compliance. Discoveries become queue items the SAME turn they're discovered, not "next time".
3. **Reorder freely.** Owner or Claude can reorder via Edit. The new top runs next.
4. **DEFERRED keeps its position.** A `DEFERRED` item is skipped *without losing its slot*. The next non-deferred `PENDING` item runs first; the deferred item stays where it was so it surfaces again next time the queue is consulted.
5. **RESOLVED moves to the bottom of the active queue (not deleted).** When an item is finished, tag it `RESOLVED` and move it to the end of the active queue table (with the resolution batch / commit / one-line outcome in Notes). Keeping it visible in the active queue — not just a log line — makes the resolution traceable when the same area is touched later. Only TRULY archived items (never coming back) move to the Completed log section.
6. **REOPENED moves to the top.** If a RESOLVED item needs another iteration (regression, incomplete fix, new evidence), tag it `REOPENED` and move it to row #1 of the active queue. Update Notes with what changed.
7. **Status enum.**
   - `PENDING` — queued, not yet started.
   - `IN_PROGRESS` — actively being worked on this session.
   - `BLOCKED` — cannot proceed without an external unblock (waiting on owner / external system / dependency); explain blocker in Notes.
   - `DEFERRED` — owner-directed skip-without-loss; retains queue position.
   - `RESOLVED` — finished + moved to bottom of active queue. Stays visible.
   - `REOPENED` — was RESOLVED, surfaced as needing another iteration; moves to top.
   - `DONE-ARCHIVED` — truly done, never coming back; moved out of active queue into Completed log.
8. **One IN_PROGRESS at a time.** Like TodoWrite, only one item is `IN_PROGRESS` at any moment so the focus is unambiguous.
9. **Item identifiers stable.** Use a short kebab-case slug per item (`smc-silent-failure-rootcause`) so the same item is referenceable across turns even after reorder.
10. **No silent drops.** If an item is removed without RESOLVED tag, explain the reason in the Completed log (e.g., `dropped — superseded by Y`).
11. **Owner authority.** Owner can add, reorder, defer, resolve, reopen, or drop any item by message. Claude cannot drop an item without owner instruction.
12. **Findings become items immediately.** Anything discovered in a status report ("X was supposed to drop but didn't / Y stayed at 100 / Z needs investigation") becomes a queue row that same turn. Don't let findings dangle in chat history.

---

## Active queue (top = next; RESOLVED items sink to the bottom of this table, not removed)

| # | Slug | Item | Status | Notes |
|---|---|---|---|---|
| P10 | `producer-polygon-news-sentiment` | **Signal producer (Batch 451)**: wire prefetched Polygon news (454MB, sentiment column) to screener | PENDING | Data is cached at `data_prefetch/polygon/news/<ticker>.parquet` with `sentiment` + `insights_json` columns. **Newer rows populated, older sentiment=NaN — must handle.** Producer reads per-ticker news, computes `news_sentiment_5d` (volume-weighted recent), `news_sentiment_30d`, `news_volume_zscore_5d`. New strategies: `news_momentum_long` (sentiment ≥ +0.5 AND volume z-score ≥ 1.5 AND breakout), `news_reversal_short` (sentiment ≥ +0.7 AND price up >10% in 5d → overreaction fade). Sleeve adds: `<base>_with_news_bull`. Academic: Tetlock 2007 / Da-Engelberg-Gao 2011. **Highest-impact prefetched-but-unused signal.** Pre-req for R4 if shipping. |
| P11 | `producer-cftc-cot-positioning` | **Signal producer**: CFTC COT for macro/ETF tier-overlay | PENDING | Data cached `data_prefetch/cftc/cot_*.parquet` for SP500/NDX/RUT/DJI/gold/copper/DXY/EUR/USD/JPY/USD. Producer computes `cot_commercials_percentile_<series>` over 3y rolling. Use: regime overlay tier modifier when broad-market commercials extreme. Limited per-stock value; mostly affects ETF strategies + portfolio tilt. Lower priority than P10/P12/P14/P16. |
| P12 | `producer-apewisdom-stocktwits-retail-sentiment` | **Signal producer**: retail social sentiment | PENDING | Apewisdom global parquet (192KB) has ticker, mentions, upvotes, rank_24h_ago. Stocktwits per-ticker (24MB). Producer emits `apewisdom_rank`, `apewisdom_rank_delta_24h`, `stocktwits_bull_bear_ratio`. New strategies: `meme_momentum_long` (rank top-10 + improving), `meme_fade_short` (rank=1 + plateau = crowded squeeze ready to reverse). Real-world precedent: Bloomberg Reddit/StockTwits Indices. |
| P13 | `producer-pytrends-search-volume` | **Signal producer**: Google search volume (low priority) | PENDING | Data cached `data_prefetch/pytrends/<ticker>.parquet`. Producer emits `search_volume_zscore_30d`. Strategy `search_spike_momentum_long`. Mixed academic signal-to-noise; ship after P10/P12/P14/P16. |
| P14 | `producer-insider-quality-differentiation` | **Signal producer**: extend `insider_signal()` to use existing Quiver role + size columns | PENDING | **Batch 453 audit correction (owner-prompted):** prior version of this row claimed SEC EDGAR Form 4 was the data source. WRONG. SEC EDGAR `data_prefetch/sec_edgar/4/AAPL.parquet` is FILING INDEX only (cols: `ticker, cik, form, filing_date, accession_number, primary_doc`). The actual Form 4 content is in XBRL `primary_doc` XML files. **Quiver IS the decoded Form 4** — its per-ticker parquet has `officerTitle`, `isDirector`, `isOfficer`, `isTenPercentOwner`, `TransactionCode` ('P'/'S'/etc.), `Shares`, `SharesOwnedFollowing`. **And `insider_signal()` ALREADY uses some of it** — CEO regex (line 422), cluster buy/sell, transaction-code filtering. **Real gap:** CFO regex missing (only CEO captured); `isDirector` column unused in signal output; transaction-size weighting absent (5K shares == 50K shares today); sold-fraction (`Shares / SharesOwnedFollowing`) weighting absent. **Action:** extend `insider_signal()` to emit additional signal levels: `cfo_buy` (regex `CFO|Chief Financial`), `director_only_buy`, `large_dollar_buy` (PricePerShare × Shares > $1M), `concentrated_sell` (sold-fraction > 50% of holdings). NO SEC EDGAR involvement. Academic ref still Cohen-Malloy-Pomorski 2012 but data source is Quiver. |
| P17a | `prerequisite-sec-edgar-xml-extractor` | **Pre-req for P17b/c**: SEC EDGAR cache is filing index only; need XML parser to extract holder identity / item codes from `primary_doc` | PENDING | **Batch 453 audit correction (owner-prompted):** SEC EDGAR cache at `data_prefetch/sec_edgar/{4, 8_K, SC_13D, SC_13G}/<ticker>.parquet` has only filing-level metadata (`ticker, cik, form, filing_date, accession_number, primary_doc`). The actual filing CONTENT (SC 13D holder identity + % owned; 8-K item codes 1.01/5.02/8.01; Form 4 transaction details) is in XBRL `primary_doc` XML files referenced by URL. **Action:** build `scripts/extract_sec_edgar_xml.py` — for each row in cache, fetch + parse the primary_doc, extract relevant fields into a denormalized parquet (`data_prefetch/sec_edgar_decoded/<form>/<ticker>.parquet`). Volume: ~6,056 filings × 2,000 tickers × 4 forms = ~48M XML files (or fewer if Sprint 0A only prefetched a subset). Likely 1-2 day batch processing job. **This pre-req unblocks SC 13D / SC 13G / 8-K signal consumption.** For Form 4, this step is REDUNDANT because Quiver already provides the decoded data — skip Form 4 extraction. |
| P17b | `signal-sc-13d-activist-sleeve` | **Ship 1: SC 13D activist filing → sleeve overlay** (`<base>_with_13d_activist_30d`) | PENDING | **Blocked-by:** P17a. **Why valuable:** Brav-Jiang-Partnoy-Thomas 2008 *Journal of Finance* +6.8% abnormal return in 30d window around 13D filing; Bebchuk-Brav-Jiang 2015 sustained +3-5pp/yr alpha for 5 years post-filing. Filers: Icahn, Ackman, Peltz, Elliott, ValueAct, Starboard. **NOT in Quiver** (sec13f is quarterly holdings with 45d lag, not activist intent). **Source after P17a:** `data_prefetch/sec_edgar_decoded/SC_13D/<ticker>.parquet` cols (expected post-extractor): `ticker, filing_date, filer_identity, percent_owned, item_4_purpose_text`. **Consumer:** new boolean `sc_13d_filed_within_30d` exposed by `sec_catalyst_signal()`; sleeve strategy variants `<base>_with_13d_activist` (e.g. `value_breakout_with_13d_activist`, `cup_handle_with_13d_activist`) compete in cube as independent cells. **Honest cost:** rare events (~50-200/yr universe-wide; ~5-10% of tickers/yr see any 13D activity) → likely fails DEC-426 n≥30 as standalone; sleeve overlay is the right pattern. **Smallest code, biggest per-event impact — ship first.** |
| P17c | `signal-8k-item-1-01-merger-target-strategy` | **Ship 2: 8-K Item 1.01 (material agreement) → standalone strategy `m_and_a_target_long`** | PENDING | **Blocked-by:** P17a + item-code text parser. **Why valuable:** Pawliczek-Skinner 2018 *Review of Accounting Studies* — Items 1.01 + 2.02 predict short-term returns (~2-3pp 10-day CAR). Item 1.01 is frequently the FIRST public disclosure that a company is being acquired or signed a major partnership — stock often gaps 10-30% on next bar. **NOT in Quiver.** **Source after P17a + item-code parser:** `data_prefetch/sec_edgar_decoded/8_K/<ticker>.parquet` cols (expected): `ticker, filing_date, item_code (1.01/2.01/2.02/5.02/...), item_body_text_summary`. **Consumer:** `8k_item_1_01_filed_within_30d` boolean → new standalone strategy `m_and_a_target_long` (enters next bar after 8-K Item 1.01 if no merger-already-announced exclusion). **Cost:** Item 1.01 frequency moderate (companies file ~1-3 per year doing material deals); high signal-to-noise. **Needs item-code body parsing** beyond the form-type extraction — `primary_doc` XBRL contains item codes in document body, not in filing-index. P17a extractor must include item-code field for 8-K specifically. |
| P17d | `signal-8k-item-5-02-officer-change-event-overlay` | **Ship 3: 8-K Item 5.02 (officer departure/election) → 7-day event overlay (downweight tier)** | PENDING | **Blocked-by:** P17a + item-code text parser (same as P17c). **Why valuable:** CEO/CFO departure carries high uncertainty premium; sudden CFO exits especially predictive of accounting issues. Often precedes downward earnings revision. Academic: prior Form 8-K studies cited above. **NOT in Quiver.** **Source after P17a:** same as P17c (8_K decoded cache). **Consumer:** `8k_item_5_02_filed_within_7d` boolean → event overlay in `_assign_confidence_tier` (downgrade preliminary tier by 1 slot for affected ticker during 7-day window). NOT a sleeve strategy or standalone; pure tier modifier. **Cost:** Item 5.02 frequency: ~1-2 per year per ticker; high coverage; moderate per-event signal. |
| P17e | `signal-sc-13g-passive-flow-overlay` | **Ship 4: SC 13G (passive 5%+ filing) → low-priority flow overlay** | PENDING | **Blocked-by:** P17a. **Why moderate value:** passive flow ≠ stock-picking edge; Vanguard/BlackRock crossing 5% predicts index reweighting + forced index-fund buying. Smart-passive concentration (Sequoia, Wellington, GMO) signals quality. **Best per-event signal is the 13D→13G TRANSITION** (activist gave up = sell) per Bebchuk-Brav-Jiang — but that requires joining 13D + 13G filings by filer identity, harder than single-form detection. **NOT in Quiver.** **Source after P17a:** `data_prefetch/sec_edgar_decoded/SC_13G/<ticker>.parquet` cols (expected): `ticker, filing_date, filer_identity, percent_owned`. **Consumer:** `sc_13g_filed_within_30d` boolean → +1 to `smart_money_score()`. LOW priority per recommendation — ship after P17b/c/d land and prove value. **Cost:** higher volume than 13D (500-2000/yr universe-wide); lower per-event signal. The 13D→13G transition variant (high-signal) is deferred until owner approves the extra extractor work. |
| P15 | `producer-finra-short-interest` | **NEW data source + producer**: FINRA short interest / days-to-cover | PENDING | **Not currently prefetched.** Add new prefetch source: FINRA biweekly short interest CSV (free download). Cache to `data_prefetch/finra/short_interest/<ticker>.parquet`. Producer emits `short_interest_pct`, `days_to_cover = SI / ADV`. New strategies: `squeeze_setup_long` (high SI + bullish breakout), `short_borrow_trap_avoid` (DTC > 5 = hard-to-borrow filter for short strategies). Academic: Cohen-Diether-Malloy 2007. |
| P16 | `producer-quiver-alt-data-overlays` | **Signal producer**: unconsumed Quiver alt-data | PENDING | Quiver prefetched 602MB — but only `congressional` + `insider` are consumed by current `smart_money.py`. **Unconsumed alt-data signals**: `housetrading`, `lobbying`, `gov_contracts`, `patentmomentum`, `offexchange`, `corporatedonors`. Each is a research-validated alpha factor. Producer batch wires the highest-value: `housetrading_signal` (member of Congress trade alignment per Ziobrowski 2004 +6pp annual alpha), `lobbying_score` (Hill-Kelly-Lockhart 2014 +0.8pp/month alpha on heavy lobbiers), `gov_contracts_change` (new federal contract = revenue boost predictor). |
| SM1 | `smart-money-sleeve-strategies` | **Smart money expansion (Batch 450)**: 10 new sleeve strategies that gate entry on smart money signal | PENDING | Per owner Batch 450 recommendation: smart money currently sizing-only. Create `<base>_with_smart_money_long` variants for top-10 base strategies (e.g. `pead_long_with_congressional_buy`, `bollinger_tight_with_insider_cluster`, `xs_momentum_with_institutional`, `mfi_oversold_with_insider`). Add to `ALL_STRATEGIES`; bring active count 185 → 195. R4 cube replay will rank sleeve cells vs base cells empirically — if criterion #7 (smart money lift ≥3pp WR) is real, sleeve cells outperform. **Does NOT filter existing strategies** — additive. Pre-req before R4 launch. |
| SM2 | `exit-smart-money-reversal` | **Smart money expansion**: 26th exit method - exits on smart money signal flip during hold | PENDING | Per owner Batch 450 recommendation: currently exits ignore smart money. If 3+ insider sells appear within 5d while strategy is long, AVOID rule logic should apply to HOLD too. Add `exit_smart_money_reversal` to `EXIT_STRATEGIES` (25 → 26 methods). Cube R4 replay will rank vs `regime_flip` / `atr_trail_1x` / etc. **Empirical test of "is smart-money flip a useful exit signal?"** Ship before R4. |
| M1 | `missing-inter-strategy-correlation` | **Missing analysis - INTEGRATE per owner Batch 450 directive A**: cube measures confluence count but never measures whether strategies are independent or redundant | PENDING | When N strategies fire on the same (ticker, date), we use raw N as confluence. If 5 of them are surface forms of the same underlying pattern (e.g., `52w_high_breakout` + `near_52w_high` + `breakout_retest` + `momentum_breakout` + `cup_handle_breakout`), they're 1 signal disguised as 5. **Action:** compute pairwise correlation matrix across strategies on their entry-day-firing series; cluster strategies with corr > 0.8; report to owner; consider down-weighting in `strategy_count` for tier assignment. |
| M2 | `missing-capacity-analysis` | **Missing analysis**: no AUM-scaling estimate per cell | PENDING | A cell with Sharpe 1.0 on $50k positions might collapse to Sharpe 0.2 on $5M. We never measure capacity. **Action:** for each PASS cell, compute (avg ADV at entry) / (position dollar size); flag cells where position size > 0.1% of ADV; surface as `capacity_$AUM_safe` field per cell. |
| M3 | `missing-random-walk-adversarial-baseline` | **Missing analysis**: never verified strict 5-Gate produces ~0 PASS cells on noise | PENDING | Feed synthetic random-walk data (no edge) through the full optimizer pipeline. Strict 5-Gate should produce 0 PASS cells. If we get >0, the verdict pipeline is overfit and current "PASS" cells are partly noise. This is the adversarial counterpart to item #4 (PSR-False) — that one prevents true PASSes; this one detects false PASSes. **Action:** synthetic-data harness in tests; assert `strict_pass_count_on_random_walk == 0`. |
| M4 | `missing-final-oos-holdout` | **Missing analysis**: DEC-505 walk-forward uses all data 2020-2026; no untouched holdout | PENDING | Walk-forward is in-period CV. Final-OOS holdout (e.g., last 6 months never looked at during optimization) is a stricter test. We currently have NO data the optimizer hasn't seen. Even Batch 414 STRATEGY_EXIT_OVERRIDE was derived from the cube data that the walk-forward then evaluates. Information leak risk. **Action:** carve `2026-01-01 → 2026-06-30` as final holdout; lock it; only inspect once before 1A-α gate decision. |
| M5 | `missing-polygon-news-sentiment` | **Missing signal consumption**: ~1.05M Polygon news articles cached, no producer feeds the screener | DEFERRED | **Owner Batch 450 directive B**: deferred — likely consumed by TradingAgents in Phase 1B+. Surface again at 1B-α planning. | Per CLAUDE.md Sprint 0A: Polygon news prefetched (per conversation summary "1.05M news articles cached" as of Pass 53). `news_sentiment_shift_during_hold` exists as exit_by_* breakdown but no strategy consumes a news-sentiment signal at entry. Major free-tier-cached edge unconsumed. **Action:** compute per-ticker daily sentiment score from Polygon news; wire as a screener-callable producer; pilot 2-3 sentiment-overlay strategies. |
| M6 | `missing-earnings-surprise-magnitude` | **Earnings surprise producer + PEAD-extension sleeve (lifted from DEFERRED per owner Batch 451)** | PENDING | **Owner Batch 451 directive: lift NOW.** Data fully cached in `data_prefetch/finnhub/calendar_earnings.parquet` (cols: epsActual, epsEstimate, revenueActual, revenueEstimate). Producer `backtest/signals/earnings_surprise_producer.py` reads Finnhub → `eps_surprise_pct = (epsActual − epsEstimate)/abs(epsEstimate)` + `revenue_surprise_pct`. Existing PEAD strategy (`backtest/signals/pead.py`) is in active roster. NEW sleeves: `pead_long_high_surprise_only` (entry filter: surprise > +5%), `pead_short_negative_surprise` (entry filter: surprise < -5%). Pre-req for R4 cube. Academic backing: Bernard-Thomas 1989 PEAD. | We have the calendar (when earnings are). We DON'T have `latest_earnings_surprise_pct` or `analyst_revision_trend_30d`. Major edge for PEAD strategies — and PEAD is one of our active strategies. The producer reads quarterly EPS via `backtest/signals/pead.py::load_quarterly_eps` (cached per Batch 421) but doesn't expose surprise %. **Action:** extend PEAD producer to emit `eps_surprise_pct` and `revision_trend_30d_pct`; pilot strategies that use them as entry filters. |
| M7 | `missing-short-interest-coverage` | **Missing signal**: no days-to-cover / short interest feed | DEFERRED | **Owner Batch 450 directive B**: deferred to Phase 1B+. | Short squeezes are a real edge for short strategies — and we have ~12 short strategies in `ALL_STRATEGIES`. Short interest + days-to-cover predict the squeeze risk. Not in any prefetched signal feed. **Action:** add FINRA/short-interest source to data_prefetch; wire as producer; pilot squeeze-detection short-side filter. |
| M8 | `missing-dynamic-strategy-retirement` | **Missing decision rule**: `project_no_apriori_strategy_pruning` says empirical only — but no trigger exists for empirical retirement | DEFERRED | **Owner Batch 450 directive C**: deferred post-1A-β / pre-next-phase. Decision-quality work — relevant when transitioning out of cube-optimization mode into deployment-prep. | The standing rule prevents A-PRIORI pruning. It doesn't define WHEN empirical retirement fires. Currently 25 all-negative-Sharpe strategies sit on the queue (#11) waiting for owner decision because there's no rule. **Action:** propose criterion (e.g., "rolling 12-month Sharpe < 0 across 2 consecutive months AND n ≥ 50" → retire) + dynamic check in walk-forward output. Closes queue item #11 with a rule instead of a one-off decision. |
| M9 | `missing-correlation-aware-confluence` | **Missing decision rule**: tier assignment uses raw `strategy_count`, not correlation-adjusted | PENDING | Depends on M1 (must compute correlations first). Then `_assign_confidence_tier` should use `effective_strategy_count` = number of independent clusters firing, not raw count. A trade with 5 firing strategies from 1 cluster gets tier MEDIUM (cluster_count=1), not HIGH (raw=5). **Action:** ship after M1; refactor `_assign_confidence_tier`. |
| M10 | `missing-realistic-slippage-model` | **Missing analysis**: `_cost_sensitivity_sharpe` uses flat bps haircut — not ADV-dependent | DEFERRED | **Owner Batch 450 directive C**: deferred post-1A-β / pre-next-phase. Decision-quality work — relevant for deployment realism, not cube-best-cells identification. | Real slippage = f(order_size / ADV, spread, volatility). Flat 5/10/20 bps misrepresents both extremes. **Action:** extend `_cost_sensitivity_sharpe` to take ADV + spread + position size; produce realistic slippage haircut per trade. Then re-run the cost-sensitivity Sharpe with realistic model. |
| M11 | `missing-pnl-concentration-hhi-per-cell` | **Missing analysis**: no concentration metric per cell | PENDING | A cell with Sharpe 1.5 might be driven by 1 outsized trade. HHI on PnL contribution per trade flags this. **Action:** Tier B (queue item #5) — add `pnl_concentration_top1_pct` and `pnl_hhi` per cell. |
| 0a | `pattern-3-rr-vs-profit-factor` | **Pattern 3 audit**: 5-Gate `rr_>=_2.0` uses `profit_factor` as proxy for R:R; mathematically different | PENDING | **Audit finding 2026-05-29 (Batch 448 broad Pattern 3 sweep):** the gate dict key is `"rr_>=_2.0"` but the code asserts `stats["profit_factor"] >= GATE_RR_MIN`. R:R = avg_win / abs(avg_loss); profit_factor = sum_wins / sum_losses = (WR/(1-WR)) × R:R. For WR=60%, profit_factor=2.0 implies R:R=1.33, NOT 2.0. Either (a) intentional convention conflation (one common usage treats them interchangeably) or (b) bug that lets cells pass a gate they should fail. **Action:** confirm intent with owner; if R:R was meant, separate `_cell_stats` to compute `avg_win / abs(avg_loss)` explicitly. Semantic-integration test required either way. |
| 0b | `pattern-3-bonferroni-denominator` | **Pattern 3 audit**: `_dec426_verdict(m_total_candidates=1)` default — Bonferroni M may be 1 (no correction) at runtime | PENDING | **Audit finding 2026-05-29:** `_dec426_verdict(stats, m_total_candidates=1)` has default M=1 which means NO multiple-testing correction (Bonferroni p = raw p). Per DEC-426 the M should be the full cube cell count per regime (~4,625) so the correction is real. **Action:** trace every callsite of `_dec426_verdict`; assert M >= cube_cell_count_per_regime is passed in production paths; add semantic-integration test "Bonferroni denominator passed at runtime > 1000". If the production path is passing M=1, the strict gate has been effectively bypassed. |
| 0c | `pattern-3-walk-forward-chronological-split` | **Pattern 3 audit**: verify `walk_forward_batch414_cells.py` IS/OOS folds actually respect time-ordering | PENDING | **Audit finding 2026-05-29:** the 1A-alpha gate hinges on OOS Sharpe ≥ 0.7 from this script. Tests check that 4 folds exist + JSON output is valid. NO test asserts that fold N's OOS year is strictly AFTER fold N's IS window, or that the 4 OOS folds are non-overlapping disjoint years. A random shuffle bug would produce a "valid" JSON with garbage OOS numbers. **Action:** read the fold-construction code; add semantic-integration test "for each fold, max(IS.entry_date) < min(OOS.entry_date)" + "OOS year sets are disjoint". |
| 0d | `pattern-3-liquidity-floor-actual-enforcement` | **Pattern 3 audit**: DEC-321/366 ADV floors exist in config; verify engine actually filters candidates by them | PENDING | **Audit finding 2026-05-29:** SKIPPED_AT_ENGINE finding #2 suggests `liquidity_floor` is the dominant reason for 47 quiet strategies. Tests verify the floor VALUES are present in `backtest/config.py`. Don't verify the engine ACTUALLY rejects candidates whose ADV is below the floor at trade time. A bug where the floor check returns True regardless (e.g., NaN-comparison) would silently pass. **Action:** synthetic-data test: feed a candidate with `adv = floor - 1`, assert it is rejected with `skip_reason = liquidity_floor`. |
| 0e | `pattern-3-survivorship-haircut-consumption` | **Pattern 3 audit**: `trade_log.survivorship_haircut` column exists; is it consumed by ANY downstream metric? | DEFERRED | **Owner Batch 450 directive C**: deferred post-1A-β / pre-next-phase. | **Audit finding 2026-05-29:** trade_log emits the column. metrics.py has no obvious `survivorship_haircut` consumer. If 0 rows trigger the haircut (e.g., because delisted-name detection silently returns False), the column is dead data. **Action:** count `survivorship_haircut != 0` rows in current cube; if 0, the haircut feature is non-functional; if non-zero, verify downstream Sharpe / WR computations actually adjust for it. |
| 0f | `pattern-3-regime-classifier-correctness` | **Pattern 3 audit**: regime classifier returns a string per trade; is the LABEL correct? | PENDING | **Audit finding 2026-05-29:** engine emits `regime` per trade. Tests verify the classifier function returns one of {bull, neutral, bear, crisis}. Don't verify the LABEL is correct (2022 should be `bear_correction_2022`, etc.). A mislabeling bug would corrupt per-regime verdicts silently. **Action:** sanity-check pinned dates: covid_crisis_2020 trades should be tagged `crisis`; strong_bull_2023 trades should be tagged `bull`. If wrong, the per-regime PASS pill on Tab 2 is unreliable. |
| 0g | `pattern-3-cost-sensitivity-sharpe-consumption` | **Pattern 3 audit**: metrics.py computes Sharpe @ 0/5/10/20 bps; is any decision driven by these? | DEFERRED | **Owner Batch 450 directive C**: deferred post-1A-β / pre-next-phase. Becomes relevant alongside M10 realistic-slippage when transitioning to deployment-realism. | **Audit finding 2026-05-29:** `_cost_sensitivity_sharpe` is in `metrics.py` and called per strategy. Computed values surface in `backtest_results.csv` columns but no apparent downstream consumer drives a strategy-deployment decision based on the 20bps Sharpe. If 20bps Sharpe < 0 means the strategy is execution-cost-fragile and shouldn't deploy live; that gate doesn't exist anywhere. **Action:** decide whether 20bps Sharpe should be a deployment gate (similar to 9-criteria) or just informational. If gate, add to DEC-426 5-Gate as a 6th gate. |
| AU1 | `5-pattern-audit-cube-populator-psr-placeholder` | **Pattern 1 #2 hit**: `backtest/results/cube_populator.py:159` has the SAME PSR placeholder as `optimize_strategies_from_cube.py:130` (queue item #4) | PENDING | **Batch 455 5-pattern audit:** verified line 109 `psr` defined in CUBE_COMBO_COLUMNS + line 159 placeholder `psr (placeholder; full DEC-247 PSR via deflated_sharpe.py wires the real one)`. Same code-level bug in a second file. **Fix:** same as #4 (wire `_deflated_sharpe` from metrics.py). Both files must be patched in the same commit. |
| AU2 | `5-pattern-audit-silent-exception-sweep-critical-paths` | **Pattern 1 #3 hit**: 132+ silent `except: pass` swallows in critical engine/signals/results code | PENDING | **Batch 455 5-pattern audit:** count per critical file = backtest.py 30 / writer.py 30 / screener.py 25 / exit_strategies.py 15 / metrics.py 13 / exit_context.py 11 / smc_ict.py 8 / cpcv.py 8. **Each silent except is a potential Pattern 1 / Pattern 3 issue** — code runs, failure invisible. `smc_ict.py` is THE file where SMC silent-failure of queue item #3 lives. **Action:** sweep all 132 silent excepts, classify each as (a) defensive-justified (e.g. file-not-exist fallback), (b) needs rate-limited diagnostic logging (like Batch 416 did for screener.py producers), (c) should re-raise. Apply Batch 416 `_log_silent_failure` pattern to all (b). **Promotes #95/#98/#99 codified rules to action: silent except = wired-not-consumed at runtime.** |
| AU3 | `5-pattern-audit-verification-matrix-scripts-layer-extension` | **Pattern 4 confirmed**: VERIFICATION_MATRIX canonical backtest is `run_phase1a.py --tickers AAPL --start 2023-01-01 --end 2023-06-30` — does NOT cover any of `scripts/*` (optimizer, walk-forward, merge, dashboard build, AWS orchestration) | PENDING | **Batch 455 5-pattern audit:** confirmed `scripts/optimize_strategies_from_cube.py` returns 0 grep hits in VERIFICATION_MATRIX.md. The matrix tracks 731 items with `engine: YES/NO/LAZY-WIRED/FUNC-DEAD` — but ALL of `scripts/*` is out of scope by definition. **Promotion of queue item #0 (audit-meta-fix-scripts-layer):** extend `scripts/build_verification_matrix.py` to include a second canonical run = `python -m coverage run scripts/optimize_strategies_from_cube.py --input-dir output_batch395_final --output-dir /tmp/optimizer_test` + merge the coverage data. Adds optimizer + walk-forward + merge + dashboard build to the matrix's engine-consumption column. **Closes Pattern 4 for the cube optimizer call path.** |
| AU4 | `5-pattern-audit-equity-curve-parallel-universe-investigation` | **Pattern 5 candidate**: `equity_curve` computed in 5 files — verify same definition or surface differences | PENDING | **Batch 455 5-pattern audit:** files that compute or modify equity_curve: `backtest/results/metrics.py` (in `compute_strategy_metrics`), `backtest/results/quant_audit.py`, `backtest/results/writer.py`, `backtest/engine/backtest.py`, `backtest/engine/portfolio.py`. **Open question:** do these compute the same thing, or 5 different definitions? Same surface form (cumulative product of pnl% per trade) may diverge on (a) compounding base (10000 vs 100000 vs %), (b) inclusion/exclusion of costs, (c) ordering (chronological vs strategy-group), (d) drawdown calculation. **Action:** read each computation; either consolidate to one source-of-truth function in metrics.py + caller imports, or document the intentional divergence. **Same lesson as `_cell_stats` parallel-universe (queue item #5).** |
| AU5 | `5-pattern-audit-portfolio-metrics-parallel-universe-investigation` | **Pattern 5 candidate**: `portfolio_metrics` computed in 6 files | PENDING | **Batch 455 5-pattern audit:** files: `backtest/results/metrics.py`, `backtest/results/writer.py`, `scripts/build_dashboard_phase_1a.py`, `scripts/merge_batch_outputs.py`, `scripts/run_t0_close_out.py`, `scripts/verify_batch_69_phase_3.py`. Two of these (build_dashboard + merge_batch_outputs) are post-processing readers — probably benign. The other four likely have their own computation paths. **Action:** same as AU4 — read each, consolidate or document. |
| AU6 | `5-pattern-audit-semantic-integration-test-framework` | **Pattern 3 mitigation**: add semantic-integration test framework to backtest/tests/ | PENDING | **Batch 455 5-pattern audit:** current tests pin file existence + count + label presence. Few assert meaningful invariants. **Mandate from CHECKLIST #100:** every queue item ships through full pyramid. **Action:** create `backtest/tests/test_semantic_integration_pyramid.py` with assertions like (1) "synthetic random-walk data through optimizer → strict 5-Gate pass count = 0" (closes M3 finding), (2) "synthetic +1% drift data → at least one strict-5-Gate-pass cell" (positive control), (3) "Bonferroni denominator passed at runtime > 1000" (closes 0b finding), (4) "fold IS/OOS time-ordering respected" (closes 0c finding), (5) "PSR computed not hardcoded" (closes #4 + AU1 findings). This single test file would close 5 audit gaps. |
| 0 | `audit-meta-fix-scripts-layer` | **META-FINDING + placeholder sweep**: extend `feedback_wired_means_engine_consumed` + VERIFICATION_MATRIX coverage + semantic-integration tests to the `scripts/*` layer; resolve all `# placeholder` debt | PENDING | **Audit-of-audits finding 2026-05-29 (Batch 447):** the PSR-hardcoded-False bug (item #4) + the `_cell_stats` vs `metrics.py` parallel-universe (item #5 background) are both instances of patterns the project already codified — `feedback_wired_means_engine_consumed` (wired = consumed, not greppable), DEC-507 wiring matrix (data DEC + toolkit DEC ≠ integration), VERIFICATION_MATRIX (coverage-driven ground truth). Those lessons were scoped to `backtest/*` only; `scripts/optimize_strategies_from_cube.py` was never re-audited under the same lens. **Placeholder sweep (`grep -rn "placeholder" scripts/ backtest/`) surfaced 14 hits**; the live-impact ones (not test fixtures, not stale comments) are: (1) `scripts/optimize_strategies_from_cube.py:130` PSR=False — already queued as item #4; (2) **`backtest/results/cube_populator.py:159` SAME PSR placeholder in a SECOND file** — same fix needed; (3) `backtest/engine/exit_context.py:268, 293, 335, 420` — `exit_regime` defaults to entry regime, so `exit_regime_flip` and any cell that depends on "regime changed since entry" cannot fire correctly; (4) `scripts/run_phase_1b_alpha_smoke.py:129` — Phase 1B-α smoke hardcodes `regime: "bull"` so its smoke output is regime-blind. **Actions:** (a) extend VERIFICATION_MATRIX to include `scripts/*` coverage; (b) add a semantic-integration test "on synthetic data with known edge, optimizer produces ≥1 strict-5-Gate-pass cell" — would have caught PSR placeholder on day one; (c) one queue row per live-impact placeholder so each gets explicit RESOLVED tag; (d) `# placeholder` becomes a banned pattern in preflight after this sweep is closed — same way Unicode em-dash is banned. **Lift to row 0** above item #1 — this is the meta-prevention layer everything else benefits from. |
| 1 | `cell-count-static-100-r3-finding` | Investigate why L2 cell count stayed at 100 in R3 (Batch 442 prediction wrong) | PENDING | R3 finding: Batch 442 predicted L2 cell count would RISE with Batches 414/415/416 active (degenerate-cell-filtering theory). It didn't — R3 produced 100 cells, same as R2. 32 cells turned over (new + retired) but total held flat. Some other filter mechanic is at play in `optimize_strategies_from_cube.py::analyze_exit_methods`. Action: read the L2 build path in detail (cube replay → groupby → n filter); count how many candidate cells exist BEFORE the n≥5 filter; identify the actual binding constraint. If it's `--no-portfolio-cap` capping trades-per-cube vs trade count being naturally that flat. |
| 2 | `skipped-bucket-static-47-r3-finding` | Investigate why SKIPPED_AT_ENGINE bucket barely moved (48 → 47) in R3 | PENDING | R3 finding: `--no-portfolio-cap` + `--no-event-suppression` should have collapsed SKIPPED if those gates were the main blockers. Staying at 47 means the remaining cause is structural — liquidity floor (DEC-321/366 ADV minimums) or `tier_too_low` from `_assign_confidence_tier`. Action: open `output_batch395_final/skipped_trades.csv`, group by `skip_reason`, count per reason; for the 47 strategies in this bucket, identify which reason dominates each. Then propose per-strategy tier-move or liquidity-floor tuning. |
| 3 | `smc-silent-failure-rootcause-r3-finding` | Inspect Batch 416 silent-producer engine logs for SMC root cause | PENDING | R3 finding: PRODUCER_LAYER_ZERO_LIKELY stayed at 6 (all SMC strategies). Batch 416 wired `_log_silent_producer_failure` at the `smc_ict` call site to surface the AWS-environment failure mode — but the logs need to be inspected. Action: download `engine.log` from S3 / the cube AMI; grep for `_log_silent_producer_failure` lines; first SMC failure should contain the exception class + traceback. Then fix the AMI/numba/smartmoneyconcepts install issue and queue R4. |
| 4 | `cube-cell-psr-hardcoded-false-P0` | **P0 BUG**: PSR gate in `optimize_strategies_from_cube.py::_dec426_verdict` is hardcoded `False`; strict 5-Gate can never pass | PENDING | **Audit finding 2026-05-29 (this turn):** line 130 of `optimize_strategies_from_cube.py` has `"psr_>=_0.95": False,  # placeholder; full PSR via deflated_sharpe.py (DEC-247)` — never replaced. Because `five_gate_pass = all(gates.values())` and gates includes PSR=False, **EVERY cell has `five_gate_pass=False` by construction**, verified across 100 R3 cells. The dashboard's PASS pill is actually 4-of-5 (verdict excludes PSR in the `all([...])` on line 134). **This is unrelated to data quality** — it's a code placeholder that ships incorrect output. **Fix:** wire `_deflated_sharpe(sharpe, n, skew, kurt)` from `metrics.py` into `_cell_stats` and replace the False with the computed `psr >= 0.95`. Re-run optimizer; expected: strict 5-Gate pass count rises from 0 to some N. **Promoted from Tier C of cube-cell-metrics-expansion to a standalone P0** because the rest of the metrics expansion is enhancement while this is a correctness bug that invalidates the strict-PASS bucket. |
| 5 | `cube-cell-metrics-expansion` | Expand L2 cell schema with 5 tiers of additional metrics (~26 new fields) — also CONSOLIDATE with `metrics.py` to stop the parallel-universe duplication | PENDING | **Blocked-by:** items #1, #2, #3 (R3 findings) + item #4 (P0 PSR fix). **Background (audit finding 2026-05-29):** `_cell_stats` in `optimize_strategies_from_cube.py` is a self-contained reimplementation that calls ZERO functions from `metrics.py`. `metrics.py::compute_strategy_metrics` already computes Sortino / Calmar / max_DD / daily Sharpe / ADF / Chow / event-window breakdown / event-conditional WR / deflated Sharpe / cost-sensitivity Sharpe / Kelly — all for strategy-level `backtest_results.csv`. None of these reach cube cells. **Consolidation:** before adding new metrics, refactor `_cell_stats` to call `metrics.py` helpers so the SAME formulas apply at both layers (no drift, no duplicate maintenance). **Scope (per owner Batch 445 directive):** Tier A (already in metrics.py - just call them): sortino / calmar / deflated_sharpe / sharpe_at_5/10/20bps / avg_mae / avg_mfe / mfe_mae_ratio / exit_efficiency / hold_days_median/std/skew / survivorship_sharpe / capital_weighted_sharpe. Tier B (slice existing trade_log columns): wr_with/without_smart_money / wr_by_vix_bucket / wr_by_days_to_earnings / wr_by_event_window. Tier C (new computation): sharpe_ci_95 / oos_sharpe / is_oos_decay / effective_n. Tier D (composite scores): sqn / k_ratio / mar. Tier E (risk mgmt): kelly_fraction / cvar_5pct / risk_of_ruin. **Ship in tier order** (A → B → C → D → E). **Each tier must include the glossary + interpretation rule inline in Tab 13** (per owner Batch 445). Preview glossary already lives at bottom of Tab 14 Reference. |
| 6 | `cube-walk-forward-r3` | Run `scripts/walk_forward_batch414_cells.py` on R3 cube cells | PENDING | DEC-505 4-fold expanding-window walk-forward on R3 data. Prior R2 result LOCKED 1A-α gate at OOS Sharpe 0.419. If R3 produces a cell with OOS Sharpe ≥ 0.7 in ≥1 regime, gate opens and Phase 1B-α $300 Haiku budget becomes eligible. **Note:** queue item #5 Tier C consumes the output of this — wire OOS Sharpe back into L2 cells. |
| 7 | `cube-analyst-overlay` | Emit single-run analyst overlays from merged cube trade log so all dashboard tabs have data | PENDING | Cube engine writes per-cell artifacts but skips `equity_curve.parquet`, `portfolio_metrics.json`, `improvements_summary.json`, `strategy_regime_matrix.json`, walk-forward, smart-money, bootstrap, congressional. Need an "analyst" pass over `output_batch395_final/trade_log.csv` calling existing functions in `backtest/results/metrics.py` + `improvements.py` to write the missing JSONs. Then Overview / Tab 4 Equity / Tab 5 Walk-fwd / Tab 6 Smart-$ / Tab 2 Regime stop showing "No data". |
| 8 | `post-run-validation-rc1` | Investigate post-run validation rc=1 from R3 merge | PENDING | Merge bg `bd01668y9` log printed `[FAIL] post-run validation rc=1` without detail (output not captured). Files in place; S3 upload completed. Likely a validation script in `aws_batch395_merge.py` invocation chain. Action: find the validation script, re-run it on `output_batch395_final/` standalone to surface the actual failure reason. |
| 9 | `1a-alpha-gate-decision-r3` | Owner decision on 1A-α gate state after R3 walk-forward | PENDING | Requires item #6 complete. Gate currently LOCKED at OOS Sharpe 0.419 < 0.7. If R3 opens it: $300 Phase 1B-α budget eligible. If still locked: loop back to entry-side optimization (Dim A thresholds / Dim B compound restructure) and queue R4. |
| 10 | `phase-1b-alpha-launch` | Phase 1B-α agent overlay run ($300 Haiku budget) | DEFERRED | Owner-gated behind 1A-α gate (item #9). Scope: agents-only validation per owner directive 2026-05-26 — strategies and exits decided in Phase 1A-β. |
| 11 | `25-negative-sharpe-deprecation` | 25 all-negative-Sharpe strategies — deprecation decision | DEFERRED | Per owner standing rule `project_no_apriori_strategy_pruning`: empirical-only. Awaits R3+ per-cell verdicts (after item #6 walk-forward) so we have OOS data not just IS. |
| 12 | `unarchive-list-eval` | Owner unarchive-list evaluation (currently archived but might come back) | PENDING | Owner mentioned considering unarchiving a few; awaits owner pick. List of 21 archived in `archive/2026-05-28-pre-1a-alpha-gate/docs/`. |
| 13 | `cube-merge-rebuild-r3` | R3 cube re-run COMPLETE + merge + optimizer + rebuild | RESOLVED | All 5 batches `_COMPLETE` 2026-05-29; merge bg `bd01668y9` succeeded; optimizer ran into `output_optimization_candidates_2026_05_29/`; dashboard rebuilt (commit `a7d2c0a4d`); Tab 16 Cube Diff populated; Tab 17/18 added (commit `3eb546b6e`). R3 trades = 29,360 (+201 vs R2). |
| 14 | `dashboard-batch446-verify` | Verify dashboard post-Batches 430/433/434/437/438/440/441/442/443/445/446 | RESOLVED | All shipped through Batch 446 (this turn). Includes PSR-hardcoded-False audit finding + metrics.py consolidation queue item. Hard-refresh to verify. Will REOPEN if owner reports specific tab issue. |

---

## Completed log (DONE-ARCHIVED — truly closed; most recent first; one line per item)

- 2026-05-29 — `execution-queue-create` — Created EXECUTION_QUEUE.md + CHECKLIST #94 (Batch 432). Owner directive: bring order to chaos.
- 2026-05-29 — `archive-prefetch-api` — Batch 431 (a8b53cfb0): archived PREFETCH_COVERAGE_AUDIT.md + API_AUDIT.md after activity-check verified no Python read/write code refs.
- 2026-05-29 — `dashboard-batch430-fix` — Batch 430 (1af03f282): fixed buildTable `$(window).ready` SyntaxError that killed all DataTable renders + added marked.js for Tab 10 + structured render for Tab 11.
- 2026-05-29 — `archive-impl-readiness-dashboard` — Batch 429 (577177432): archived IMPLEMENTATION_READINESS_DASHBOARD.md (self-superseded).
- 2026-05-28 — `archive-batch318-quant` — Batch 428 (f4191bb7f): archived BATCH_318_PROCESS_POOL_DESIGN + QUANT_CORRECTNESS_AUDIT_DEC_246.
- 2026-05-28 — `archive-sprint1-readme` — Batch 427 (91b7cd4cd): archived SPRINT1_POLYGON_PREFETCH_README (Sprint 1 → 0A rename).
- 2026-05-28 — `archive-audit-triage-post-may29` — Batch 426 (27c512aee): archived AUDIT_TRIAGE.md + POST_MAY_29_OPERATION_GUIDE.md.
- 2026-05-28 — `archive-dated-audits-gitignore` — Batch 425 (574205bce): archived 8 dated audit docs + .gitignore additions (`backtest/agents/cache/`, `/tmp_*`, `/vm_*`) + 4 root tmp orphans deleted.
- 2026-05-28 — `cleanup-prior-outputs` — Batch (ec9c7082b): removed 735 tracked files from 17 superseded `output_*` dirs (2.4M deletion lines).
- 2026-05-28 — `monitoring-framework-update` — Batch (cc9246445): doc-sweep monitoring framework L4 stdout buffering + AWS PATH prereq + pre-launch protocol.
- 2026-05-28 — `cube-shape-a-launch` — Batches 424 (948769bf2) + parallel orchestrator: launched Shape A 3 × c7a.8xlarge spot cube re-run; all 5 batches completed 2026-05-29.

---

## Cross-references

- `CHECKLIST.md` #94 — mandatory per-turn queue update (rule 2 above).
- `MONITORING_FRAMEWORK.md` — L0-L7 stack (cube run monitor sits in L4 layer).
- `PHASE_1A_BETA_STATUS.md` — Phase 1A-β living doc (Stage 1-6 batch ship list).
- `PHASE_1A_BETA_CUBE_OPTIMIZATION_WORKFLOW.md` — locked 6-stage workflow.
- `archive/cube_rounds/rounds.json` — round-by-round registry (Batch 443) consumed by Tabs 17 + 18.
- `CLAUDE.md` HARD RULES — owner approval, no-new-md-without-approval (`feedback_no_write_only_md_files`), full-pyramid mandate (CHECKLIST #69/#93).
